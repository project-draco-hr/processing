{
  smooth=false;
  if (1 < antialias) {
    antialias=0;
    if (primarySurface) {
      restartContext();
    }
 else {
      initOffscreen();
    }
  }
  pgl.disableMultisample();
  pgl.disablePointSmooth();
  pgl.disableLineSmooth();
  pgl.disablePolygonSmooth();
}
