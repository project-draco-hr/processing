{
  flush();
  PTexture tex=getTexture(source);
  if (tex != null) {
    beginScreenOp(OP_WRITE);
    pgl.drawTexture(tex.glTarget,tex.glID,tex.glWidth,tex.glHeight,0,0,tex.width,tex.height,x,y + height,x + tex.width,y + height - tex.height);
    endScreenOp();
    pixelsAreDirty=true;
  }
}
