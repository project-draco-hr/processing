{
  beginShape(TRIANGLES);
  defaultEdges=false;
  normalMode=NORMAL_MODE_VERTEX;
  int[] indices=inGeo.addSphere(r,sphereDetailU,sphereDetailV,fill,fillColor,stroke,strokeColor,strokeWeight,ambientColor,specularColor,emissiveColor,shininess);
  endShape(indices);
}
