{
  report("top endDraw()");
  if (hints[ENABLE_DEPTH_SORT]) {
    flush();
  }
  if (drawable != null) {
    drawable.swapBuffers();
  }
  gl.glPopAttrib();
  report("bot endDraw()");
  if (drawable != null) {
    releaseContext();
  }
}
