{
  report("top endDraw()");
  if (hints[ENABLE_DEPTH_SORT]) {
    flush();
  }
  boolean success=mEglHelper.swap();
  if (!success) {
    System.err.println("Could not swap buffers.");
  }
  report("bot endDraw()");
}
