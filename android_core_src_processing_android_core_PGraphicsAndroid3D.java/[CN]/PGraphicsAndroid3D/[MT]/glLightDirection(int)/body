{
  if (lightType[num] == DIRECTIONAL) {
    gl.glLightfv(GL10.GL_LIGHT0 + num,GL10.GL_POSITION,lightNormal[num].array(),0);
  }
 else {
    gl.glLightfv(GL10.GL_LIGHT0 + num,GL10.GL_SPOT_DIRECTION,lightNormal[num].array(),0);
  }
}
