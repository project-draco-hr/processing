{
  if (image == null || ((BufferedImage)image).getWidth() != width * pixelFactor || ((BufferedImage)image).getHeight() != height * pixelFactor) {
    GraphicsConfiguration gc=null;
    if (surface != null) {
      Component comp=null;
      if (comp == null) {
        comp=parent.frame;
      }
      if (comp != null) {
        gc=comp.getGraphicsConfiguration();
      }
    }
    if (gc == null) {
      System.err.println("GraphicsConfiguration null in initImage()");
      GraphicsEnvironment ge=GraphicsEnvironment.getLocalGraphicsEnvironment();
      gc=ge.getDefaultScreenDevice().getDefaultConfiguration();
    }
    int wide=width * pixelFactor;
    int high=height * pixelFactor;
    image=gc.createCompatibleImage(wide,high);
  }
  return (Graphics2D)image.getGraphics();
}
