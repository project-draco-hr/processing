{
  if ((sw != dw) || (sh != dh)) {
    copy(this,sx,sy,sw,sh,dx,dy,dw,dh);
  }
 else {
    if (imageMode == CORNERS) {
      sw-=sx;
      sh-=sy;
    }
    dx=dx - sx;
    dy=dy - sy;
    g2.copyArea(sx,sy,sw,sh,dx,dy);
  }
}
