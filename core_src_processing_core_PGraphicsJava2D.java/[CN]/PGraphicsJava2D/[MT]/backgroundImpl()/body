{
  if (backgroundAlpha) {
    clearPixels(backgroundColor);
  }
 else {
    Color bgColor=new Color(backgroundColor);
    pushMatrix();
    resetMatrix();
    g2.setColor(bgColor);
    g2.fillRect(0,0,width,height);
    popMatrix();
  }
}
