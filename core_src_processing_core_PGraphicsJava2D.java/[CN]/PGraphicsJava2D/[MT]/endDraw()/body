{
  if (primarySurface) {
    if (canvas != null) {
      System.out.println(canvas);
      canvas.repaint();
    }
 else {
synchronized (image) {
        image.getGraphics().drawImage(offscreen,0,0,null);
      }
    }
  }
 else {
    loadPixels();
  }
  setModified();
  super.updatePixels();
}
