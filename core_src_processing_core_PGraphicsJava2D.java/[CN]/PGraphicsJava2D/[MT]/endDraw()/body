{
  if (primarySurface) {
    if (useCanvas) {
      redraw();
    }
 else     if (useOffscreen) {
synchronized (image) {
        image.getGraphics().drawImage(offscreen,0,0,null);
      }
    }
 else {
    }
  }
 else {
    loadPixels();
  }
  setModified();
}
