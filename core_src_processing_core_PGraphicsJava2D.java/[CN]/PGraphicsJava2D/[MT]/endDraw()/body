{
  if (primarySurface) {
    if (useCanvas) {
      redraw();
    }
 else {
synchronized (image) {
        image.getGraphics().drawImage(offscreen,0,0,null);
      }
    }
  }
 else {
    loadPixels();
  }
  setModified();
  super.updatePixels();
}
