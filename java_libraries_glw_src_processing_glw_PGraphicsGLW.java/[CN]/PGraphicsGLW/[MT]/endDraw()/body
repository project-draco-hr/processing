{
  report("top endDraw()");
  if (!drawing) {
    return;
  }
  if (primarySurface) {
    setCurrentPG(null);
  }
 else {
    throw new RuntimeException("GLW renderer cannot be used as an offscreen surface.");
  }
  drawing=false;
  report("bot endDraw()");
}
