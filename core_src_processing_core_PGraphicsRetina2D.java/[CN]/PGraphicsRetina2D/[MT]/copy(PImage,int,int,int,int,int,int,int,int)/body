{
  if (hints[ENABLE_RETINA_PIXELS]) {
    super.copy(src,sx,sy,sw,sh,dx,dy,dw,dh);
  }
 else {
    g2.drawImage((Image)src.getNative(),dx * 2,dy * 2,(dx + dw) * 2,(dy + dh) * 2,sx,sy,sx + sw,sy + sh,null);
  }
}
