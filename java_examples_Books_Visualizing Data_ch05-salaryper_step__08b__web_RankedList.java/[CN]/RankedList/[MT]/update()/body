{
  order=new int[count];
  for (int i=0; i < count; i++) {
    order[i]=i;
  }
  sort(0,count - 1);
  for (int i=0; i < count; i++) {
    rank[order[i]]=i;
  }
  minValue=PApplet.min(value);
  maxValue=PApplet.max(value);
}
