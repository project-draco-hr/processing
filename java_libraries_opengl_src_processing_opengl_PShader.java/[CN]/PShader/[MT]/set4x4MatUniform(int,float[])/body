{
  if (-1 < loc) {
    if (mat4x4 == null) {
      mat4x4=pgl.createFloatBuffer(4 * 4);
    }
    mat4x4.rewind();
    mat4x4.put(mat);
    mat4x4.flip();
    pgl.glUniformMatrix4fv(loc,1,false,mat4x4);
  }
}
