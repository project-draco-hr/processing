{
  if (tweakMode) {
    removeAllListeners();
    addPrevListeners();
    getJavaPainter().stopTweakMode();
    editable=true;
    caretBlinks=true;
    setCaretVisible(true);
    tweakMode=false;
  }
}
