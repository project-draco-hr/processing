{
  noSmooth();
  colorMode(RGB,255);
  fill(255);
  stroke(0);
  shape=0;
  rectMode(CORNER);
  ellipseMode(DIAMETER);
  textFont=null;
  textSize=12;
  textLeading=14;
  textAlign=LEFT;
  textMode=MODEL;
  if (primarySurface) {
    background(backgroundColor);
  }
  settingsInited=true;
}
