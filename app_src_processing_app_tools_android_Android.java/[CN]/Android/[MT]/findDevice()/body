{
  String[] devices;
  try {
    devices=Device.list();
  }
 catch (  IOException e) {
    editor.statusError(e);
    return null;
  }
  for (  String name : devices) {
    if (!name.startsWith("emulator-")) {
      return name;
    }
  }
  return null;
}
