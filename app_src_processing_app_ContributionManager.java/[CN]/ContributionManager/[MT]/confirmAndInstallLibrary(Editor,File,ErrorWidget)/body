{
  int result=Base.showYesNoQuestion(editor,"Install","Install libraries from " + libFile.getName() + "?",ContributionManager.DOUBLE_CLICK_SECONDARY);
  if (result == JOptionPane.YES_OPTION) {
    return installLibrary(editor,libFile,null,true,statusBar);
  }
  return null;
}
