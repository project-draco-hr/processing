{
  copyMatrix(src,dest);
  copyStyles(src,dest);
  copyImage(src,dest);
  for (int i=0; i < src.getChildCount(); i++) {
    PShape c=PShape3D.createShape(parent,src.getChild(i));
    dest.addChild(c);
  }
}
