{
  if (tex != null) {
    int n=PApplet.min(textures.length,tex.length);
    for (int i=0; i < n; i++) {
      textures[i]=tex[i];
    }
    for (int i=n; i < textures.length; i++) {
      textures[i]=null;
    }
  }
}
