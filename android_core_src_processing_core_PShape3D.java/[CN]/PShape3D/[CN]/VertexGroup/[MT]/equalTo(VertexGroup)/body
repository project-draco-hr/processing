{
  boolean res=glMode == gr.glMode && sw == gr.sw;
  if (!res)   return false;
  for (int i=0; i < textures.length; i++) {
    if (textures[i] != gr.textures[i]) {
      res=false;
    }
  }
  return res;
}
