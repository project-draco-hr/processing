{
  if (family == GROUP) {
    init();
    if (children == null) {
      addDefaultChild();
    }
    setTexture(0,tex0,tex1);
  }
 else {
    setTextureImpl(tex0,0);
    setTextureImpl(tex1,1);
  }
}
