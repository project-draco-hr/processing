{
  if (family == GROUP) {
    init();
    if (children == null) {
      addDefaultChild();
    }
    setTexture(0,tex);
  }
 else {
    for (int i=0; i < tex.length; i++) {
      setTextureImpl(tex[i],i);
    }
  }
}
