{
  updateTesselation();
  pgl.bindVertexBuffer(id);
  ByteBuffer bb;
  if (root == this) {
    bb=pgl.mapVertexBuffer();
  }
 else {
    bb=pgl.mapVertexBufferRange(offset,count);
  }
  return bb;
}
