{
  if (family == GROUP) {
    for (int i=0; i < childCount; i++) {
      PShape3D child=(PShape3D)children[i];
      child.specular(gray);
    }
  }
 else {
    colorCalc(gray);
    specularFromCalc();
  }
}
