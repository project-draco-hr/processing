{
  if (family == GROUP) {
    init();
    if (children == null) {
      addDefaultChild();
    }
    for (int i=0; i < childCount; i++) {
      setTexture(i,tex);
    }
  }
 else {
    setTextureImpl(tex,0);
  }
}
