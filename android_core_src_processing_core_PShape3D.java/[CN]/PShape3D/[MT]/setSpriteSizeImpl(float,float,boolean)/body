{
  float s0=PGraphicsAndroid3D.maxPointSize;
  if (linear) {
    spriteDistAtt[1]=(s0 - s) / (d * s);
    spriteDistAtt[2]=0;
  }
 else {
    spriteDistAtt[1]=0;
    spriteDistAtt[2]=(s0 - s) / (d * d * s);
  }
}
