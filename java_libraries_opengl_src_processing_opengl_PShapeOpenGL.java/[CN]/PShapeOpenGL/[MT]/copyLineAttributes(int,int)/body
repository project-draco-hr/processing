{
  pgl.glBindBuffer(PGL.GL_ARRAY_BUFFER,glLineAttribBufferID);
  pgl.glBufferSubData(PGL.GL_ARRAY_BUFFER,4 * offset * PGL.SIZEOF_FLOAT,4 * size * PGL.SIZEOF_FLOAT,FloatBuffer.wrap(tessGeo.lineAttribs,0,4 * size));
  pgl.glBindBuffer(PGL.GL_ARRAY_BUFFER,0);
}
