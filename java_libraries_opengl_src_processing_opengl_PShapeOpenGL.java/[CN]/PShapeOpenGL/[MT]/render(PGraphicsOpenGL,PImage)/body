{
  if (root == null) {
    return;
  }
  if (havePoints) {
    renderPoints(g);
    if (g.haveRaw()) {
      rawPoints(g);
    }
  }
  if (haveLines) {
    renderLines(g);
    if (g.haveRaw()) {
      rawLines(g);
    }
  }
  if (haveFill) {
    renderFill(g,texture);
    if (g.haveRaw()) {
      rawFill(g,texture);
    }
  }
}
