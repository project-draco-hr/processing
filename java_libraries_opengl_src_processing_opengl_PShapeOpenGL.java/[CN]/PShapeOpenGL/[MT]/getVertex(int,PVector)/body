{
  updateTessellation();
  if (vec == null) {
    vec=new PVector();
  }
  vec.x=inGeo.vertices[4 * index + 0];
  vec.y=inGeo.vertices[4 * index + 1];
  vec.z=inGeo.vertices[4 * index + 2];
  return vec;
}
