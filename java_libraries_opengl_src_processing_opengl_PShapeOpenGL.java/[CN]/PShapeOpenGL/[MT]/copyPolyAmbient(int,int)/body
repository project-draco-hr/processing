{
  pgl.glBindBuffer(PGL.GL_ARRAY_BUFFER,glPolyAmbientBufferID);
  pgl.glBufferSubData(PGL.GL_ARRAY_BUFFER,offset * PGL.SIZEOF_INT,size * PGL.SIZEOF_INT,IntBuffer.wrap(tessGeo.polyAmbient,0,size));
  pgl.glBindBuffer(PGL.GL_ARRAY_BUFFER,0);
}
