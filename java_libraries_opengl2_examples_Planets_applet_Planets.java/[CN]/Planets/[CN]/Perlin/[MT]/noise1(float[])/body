{
  int bx0, bx1;
  float rx0, rx1, sx, t, u, v;
  t=vec[0] + N;
  bx0=PApplet.parseInt(t) & BM;
  bx1=(bx0 + 1) & BM;
  rx0=t - PApplet.parseInt(t);
  rx1=rx0 - 1.0f;
  sx=sCurve(rx0);
  u=rx0 * g1[p[bx0]];
  v=rx1 * g1[p[bx1]];
  return lerp(u,v,sx);
}
