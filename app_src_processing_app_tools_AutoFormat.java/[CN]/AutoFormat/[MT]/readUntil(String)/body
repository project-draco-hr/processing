{
  int matched=0;
  while (matched < string.length()) {
    final char c=next();
    if (c == string.charAt(matched))     matched++;
 else     if (c == string.charAt(0))     matched=1;
 else     matched=0;
  }
}
