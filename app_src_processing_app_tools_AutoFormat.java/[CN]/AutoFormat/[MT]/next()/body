{
  if (EOF) {
    return 0;
  }
  pos++;
  if (pos < chars.length) {
    currentChar=chars[pos];
    if (!Character.isWhitespace(currentChar))     lastNonWhitespace=currentChar;
  }
 else {
    currentChar=0;
  }
  if (pos == chars.length - 1) {
    EOF=true;
  }
  return currentChar;
}
