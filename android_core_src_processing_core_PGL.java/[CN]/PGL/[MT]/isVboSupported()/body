{
  String ver=GLES20.glGetString(GLES20.GL_VERSION);
  String ext=GLES20.glGetString(GLES20.GL_EXTENSIONS);
  if (-1 < ext.indexOf("vertex_buffer_object") || -1 < ver.indexOf("1.1") || -1 < ver.indexOf("2.")) {
    return true;
  }
  return false;
}
