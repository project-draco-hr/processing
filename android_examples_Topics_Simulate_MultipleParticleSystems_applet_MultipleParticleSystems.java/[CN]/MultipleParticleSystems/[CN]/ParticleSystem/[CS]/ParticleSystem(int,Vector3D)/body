{
  particles=new ArrayList();
  origin=v.copy();
  for (int i=0; i < num; i++) {
    if (random(1) < 0.5f) {
      particles.add(new CrazyParticle(origin));
    }
 else {
      particles.add(new Particle(origin));
    }
  }
}
