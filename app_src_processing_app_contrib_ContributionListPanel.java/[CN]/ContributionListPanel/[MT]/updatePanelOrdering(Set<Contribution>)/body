{
  dtm.getDataVector().removeAllElements();
  dtm.fireTableDataChanged();
  for (  Contribution entry : contributionsSet) {
    ((DefaultTableModel)table.getModel()).addRow(new Object[]{entry,entry,entry});
  }
}
