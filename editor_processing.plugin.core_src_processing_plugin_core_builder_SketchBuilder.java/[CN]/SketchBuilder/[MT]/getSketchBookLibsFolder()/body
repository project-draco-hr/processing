{
  try {
    File folder=sketch.getLocation().removeLastSegments(1).append("libraries").toFile();
    folder.isDirectory();
    return folder;
  }
 catch (  Exception e) {
    ProcessingLog.logError(e);
    return null;
  }
}
