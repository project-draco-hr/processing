{
  if (EOF) {
    return '\0';
  }
  pos++;
  char retVal;
  if (pos < chars.length) {
    retVal=chars[pos];
    if (!Character.isWhitespace(retVal))     lastNonWhitespace=retVal;
  }
 else {
    retVal='\0';
  }
  if (pos == chars.length - 1) {
    EOF=true;
  }
  return retVal;
}
