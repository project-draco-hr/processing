{
  if (-1 < loc) {
    if (mat3x3 == null) {
      mat3x3=pgl.createFloatBuffer(3 * 3);
    }
    mat3x3.rewind();
    mat3x3.put(mat);
    mat3x3.flip();
    pgl.glUniformMatrix3fv(loc,1,false,mat3x3);
  }
}
