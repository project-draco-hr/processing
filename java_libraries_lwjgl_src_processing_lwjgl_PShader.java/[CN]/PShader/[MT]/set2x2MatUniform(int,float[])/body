{
  if (-1 < loc) {
    if (mat2x2 == null) {
      mat2x2=pgl.createFloatBuffer(2 * 2);
    }
    mat2x2.rewind();
    mat2x2.put(mat);
    mat2x2.flip();
    pgl.glUniformMatrix2fv(loc,1,false,mat2x2);
  }
}
