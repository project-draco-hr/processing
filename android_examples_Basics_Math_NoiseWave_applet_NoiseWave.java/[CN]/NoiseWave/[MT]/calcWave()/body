{
  float dx=0.05f;
  float dy=0.01f;
  float amplitude=100.0f;
  yoff+=dy;
  float xoff=yoff;
  for (int i=0; i < yvalues.length; i++) {
    yvalues[i]=(2 * noise(xoff) - 1) * amplitude;
    xoff+=dx;
  }
}
