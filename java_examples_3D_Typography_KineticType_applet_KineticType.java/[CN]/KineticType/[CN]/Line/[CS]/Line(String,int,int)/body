{
  myString=s;
  xPosition=i;
  yPosition=j;
  myLetters=new Letter[s.length()];
  float f1=0.0f;
  for (int k=0; k < s.length(); k++) {
    char c=s.charAt(k);
    f1+=textWidth(c);
    Letter letter=new Letter(c,f1,0.0f);
    myLetters[k]=letter;
  }
  curlInX=0.1f;
}
