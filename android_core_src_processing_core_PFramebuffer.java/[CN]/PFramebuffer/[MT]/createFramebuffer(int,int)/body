{
  deleteFramebuffer();
  width=w;
  height=h;
  if (screenFb) {
    glFboID=0;
  }
 else   if (FboMode) {
    glFboID=a3d.createGLResource(PGraphicsAndroid3D.GL_FRAME_BUFFER);
  }
 else {
    glFboID=0;
  }
}
