{
  release();
  if (screenFb) {
    glFboID=0;
  }
 else   if (fboMode) {
    glFboID=a3d.createGLResource(PGraphicsAndroid3D.GL_FRAME_BUFFER);
  }
 else {
    glFboID=0;
  }
  if (0 < depthBits) {
    createDepthBuffer();
  }
  if (0 < stencilBits) {
    createStencilBuffer();
  }
}
