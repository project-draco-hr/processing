{
  List<String> list=new ArrayList<String>();
  if (string != null) {
    String[] listAsArray=string.split(";");
    for (    String element : listAsArray) {
      list.add(element);
    }
  }
  return list;
}
