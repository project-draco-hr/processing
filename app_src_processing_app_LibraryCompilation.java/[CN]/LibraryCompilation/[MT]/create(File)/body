{
  try {
    LibraryCompilation compilation=new LibraryCompilation(folder);
    if (compilation.libraries.isEmpty()) {
      return null;
    }
    return compilation;
  }
 catch (  IOException e) {
  }
  return null;
}
