{
  libsFolder.mkdirs();
  File file=new File(Base.getSketchbookFolder(),Android.ANDROID_CORE_FILENAME);
  InputStream input=PApplet.createInput(file);
  PApplet.saveStream(new File(libsFolder,"processing-core.jar"),input);
}
