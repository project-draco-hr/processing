{
  if (applet != null) {
    applet.stop();
    applet=null;
  }
 else   if (process != null) {
    try {
      processOutput.write(PApplet.EXTERNAL_STOP);
      processOutput.flush();
    }
 catch (    IOException e) {
      close();
    }
  }
}
