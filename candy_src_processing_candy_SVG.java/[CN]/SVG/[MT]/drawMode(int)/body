{
  if (which == PConstants.CORNER || which == PConstants.CENTER) {
    drawMode=which;
  }
 else {
    throw new RuntimeException("Only drawMode(CENTER) and " + "drawMode(CORNER) are available.");
  }
}
