{
  if (!display)   return;
  if (!ignoreStyles) {
    drawStyles();
  }
  if (hasTransform) {
    parent.pushMatrix();
    parent.translate(translateX,translateY);
    parent.rotate(rotation);
  }
  drawShape();
  if (hasTransform) {
    parent.popMatrix();
  }
}
