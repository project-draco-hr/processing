{
  int save_s_flg;
  save_s_flg=s_flg;
  int done=0;
  c=string[j++]=getchr();
  while (done == 0) {
    c=string[j++]=getchr();
    while (c != '/') {
      if (c == '\n' || c == '\r') {
        lineNumber++;
        putcoms();
        s_flg=1;
      }
      c=string[j++]=getchr();
    }
    String tmpstr=new String(string);
    if (j > 1 && string[j - 2] == '*') {
      done=1;
      jdoc=0;
    }
  }
  putcoms();
  s_flg=save_s_flg;
  jdoc=0;
  return;
}
