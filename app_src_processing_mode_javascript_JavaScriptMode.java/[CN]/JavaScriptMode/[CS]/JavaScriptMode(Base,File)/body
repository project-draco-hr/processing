{
  super(base,folder);
  try {
    loadKeywords();
    loadAdditionalKeywords(new File(base.getContentFile("modes/java"),"keywords.txt"),tokenMarker);
  }
 catch (  IOException e) {
    Base.showError("Problem loading keywords","Could not load keywords.txt, please re-install Processing.",e);
  }
}
