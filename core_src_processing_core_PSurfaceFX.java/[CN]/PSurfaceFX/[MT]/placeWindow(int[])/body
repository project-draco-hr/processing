{
  if (location != null) {
    stage.setX(location[0]);
    stage.setY(location[1]);
  }
 else {
  }
  if (stage.getY() < 0) {
    stage.setY(30);
  }
  if (sketch.getGraphics().displayable()) {
    setVisible(true);
  }
}
