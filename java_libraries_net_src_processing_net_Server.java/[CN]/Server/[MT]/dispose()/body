{
  thread=null;
  if (clients != null) {
    while (clientCount > 0) {
      disconnect(clients[0]);
    }
    clientCount=0;
    clients=null;
  }
  try {
    if (server != null) {
      server.close();
      server=null;
    }
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
