{
  Toolkit.setIcon(this);
  setDefaultCloseOperation(WindowConstants.HIDE_ON_CLOSE);
  setBounds(100,100,458,160);
  contentPane=new JPanel();
  contentPane.setBorder(new EmptyBorder(5,5,5,5));
  setContentPane(contentPane);
  contentPane.setLayout(new BorderLayout(0,0));
  scrollPane=new JScrollPane();
  contentPane.add(scrollPane);
  errorTable=new XQErrorTable(editor);
  scrollPane.setViewportView(errorTable);
  try {
    Docker=new DockTool2Base();
    addListeners();
  }
 catch (  Exception e) {
    System.out.println("addListeners() acted silly.");
    e.printStackTrace();
  }
  if (editor != null) {
    setLocation(editor.getLocation().x + editor.getWidth(),editor.getLocation().y);
  }
}
