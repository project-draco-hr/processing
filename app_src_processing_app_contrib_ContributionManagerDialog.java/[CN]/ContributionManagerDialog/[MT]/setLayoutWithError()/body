{
  GroupLayout layout=new GroupLayout(dialog.getContentPane());
  dialog.getContentPane().setLayout(layout);
  dialog.setResizable(true);
  layout.setHorizontalGroup(layout.createParallelGroup().addComponent(statusPanel).addComponent(errorPanel).addComponent(tabbedPane));
  layout.setVerticalGroup(layout.createSequentialGroup().addComponent(tabbedPane,tabbedPane.getSize().height - errorPanel.getPreferredSize().height - 200,tabbedPane.getSize().height - errorPanel.getPreferredSize().height,GroupLayout.PREFERRED_SIZE).addComponent(errorPanel,GroupLayout.PREFERRED_SIZE,GroupLayout.PREFERRED_SIZE,GroupLayout.PREFERRED_SIZE).addComponent(statusPanel,GroupLayout.PREFERRED_SIZE,GroupLayout.PREFERRED_SIZE,GroupLayout.PREFERRED_SIZE));
  errorPanel.setVisible(true);
  dialog.validate();
  dialog.pack();
  dialog.repaint();
}
