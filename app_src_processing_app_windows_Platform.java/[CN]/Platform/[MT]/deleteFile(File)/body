{
  try {
    moveToTrash(new File[]{file});
  }
 catch (  IOException e) {
    e.printStackTrace();
    Base.log("Could not move " + file.getAbsolutePath() + " to the trash.",e);
    return false;
  }
  return true;
}
