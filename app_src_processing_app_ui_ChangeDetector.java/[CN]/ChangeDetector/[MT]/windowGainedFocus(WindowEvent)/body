{
  if (Preferences.getBoolean("editor.watcher")) {
    if (skip) {
      skip=false;
    }
 else {
      new Thread(new Runnable(){
        @Override public void run(){
          checkFileChange();
        }
      }
).start();
    }
  }
}
