{
  String iconPath="data" + File.separator + "icons";
  if (editor != null) {
    iconPath=(Base.getSketchbookFolder().getAbsolutePath()) + File.separator + "modes"+ File.separator+ "ExperimentalMode"+ File.separator+ "data"+ File.separator+ "icons";
    ;
  }
  classIcon=new ImageIcon(iconPath + File.separator + "class_obj.png");
  methodIcon=new ImageIcon(iconPath + File.separator + "methpub_obj.png");
  fieldIcon=new ImageIcon(iconPath + File.separator + "field_protected_obj.png");
}
