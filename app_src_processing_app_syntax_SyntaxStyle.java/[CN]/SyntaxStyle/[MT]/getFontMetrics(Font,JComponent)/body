{
  if (font.equals(lastFont) && fontMetrics != null) {
    return fontMetrics;
  }
  lastFont=font;
  lastStyledFont=findFont(font.getFamily(),bold ? Font.BOLD : Font.PLAIN,font.getSize());
  fontMetrics=comp.getFontMetrics(lastStyledFont);
  return fontMetrics;
}
