{
  super.textFont(which);
  if (textMode == SHAPE) {
    if (textFont.findFont() == null) {
      showError("Cannot use " + which.name + " as with textMode(SHAPE) "+ "because its native equivalent cannot be found.");
    }
  }
}
