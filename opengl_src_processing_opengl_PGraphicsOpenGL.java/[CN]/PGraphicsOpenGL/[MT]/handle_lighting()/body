{
  super.handle_lighting();
  for (int i=vertex_start; i < vertex_end; i++) {
    float v[]=vertices[i];
    v[R]=clamp(v[R] + v[SPR]);
    v[G]=clamp(v[G] + v[SPG]);
    v[B]=clamp(v[B] + v[SPB]);
  }
}
