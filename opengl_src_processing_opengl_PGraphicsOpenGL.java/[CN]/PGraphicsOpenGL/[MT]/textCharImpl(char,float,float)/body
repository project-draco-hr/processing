{
  if (textMode == SHAPE) {
    if (textFont.getFont() == null) {
      PGraphics.showError("textMode(SHAPE) is disabled because the font " + "\"" + textFont.name + "\" is not available.");
    }
 else {
      textCharImplShape(ch,x,y);
    }
  }
 else {
    super.textCharImpl(ch,x,y);
  }
}
