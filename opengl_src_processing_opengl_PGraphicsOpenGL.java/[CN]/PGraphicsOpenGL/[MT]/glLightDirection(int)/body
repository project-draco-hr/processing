{
  if (lightType[num] == DIRECTIONAL) {
    gl.glLightfv(GL.GL_LIGHT0 + num,GL.GL_POSITION,lightNormal[num].array(),0);
  }
 else {
    gl.glLightfv(GL.GL_LIGHT0 + num,GL.GL_SPOT_DIRECTION,lightNormal[num].array(),0);
  }
}
