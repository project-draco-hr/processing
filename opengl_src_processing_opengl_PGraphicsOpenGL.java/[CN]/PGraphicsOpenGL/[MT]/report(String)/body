{
  if (!hints[DISABLE_ERROR_REPORT]) {
    int err=gl.glGetError();
    if (err != 0) {
      String errString=glu.gluErrorString(err);
      System.err.println("OpenGL error " + err + " at "+ where+ ": "+ errString);
    }
  }
}
