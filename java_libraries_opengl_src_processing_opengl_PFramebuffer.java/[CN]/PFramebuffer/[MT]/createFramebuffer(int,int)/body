{
  deleteFramebuffer();
  width=w;
  height=h;
  if (screenFb) {
    glFboID=0;
  }
 else   if (fboMode) {
    glFboID=ogl.createGLResource(PGraphicsOpenGL.GL_FRAME_BUFFER);
  }
 else {
    glFboID=0;
  }
}
