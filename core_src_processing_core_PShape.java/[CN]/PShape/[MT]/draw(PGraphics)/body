{
  if (!visible)   return;
  if (drawMode == PConstants.CENTER) {
    g.pushMatrix();
    g.translate(-width / 2,-height / 2);
  }
  pre(g);
  drawImpl(g);
  post(g);
  if (drawMode == PConstants.CENTER) {
    g.popMatrix();
  }
}
