{
  if (Android.sdkPath == null) {
    JOptionPane.showMessageDialog(editor,"Before using the reset command, " + "you must first enable Android Mode.");
  }
 else {
    Android.resetServer(editor);
  }
}
