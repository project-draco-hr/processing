{
  background(102);
  if (second() % 2 == 0) {
    jitter=(random(-0.1f,0.1f));
  }
  angle=angle + jitter;
  cosine=cos(angle);
  translate(width / 2,height / 2);
  rotate(cosine);
  rectMode(CENTER);
  rect(0,0,115,115);
}
