{
  if (font == null) {
    font=PdeApplet.getFont("editor.console.font",new Font("Monospaced",Font.PLAIN,11));
    metrics=Toolkit.getDefaultToolkit().getFontMetrics(font);
    ascent=metrics.getAscent();
  }
  return new Dimension(300 + HINSET * 2,ascent * lineCount + VINSET * 2);
}
