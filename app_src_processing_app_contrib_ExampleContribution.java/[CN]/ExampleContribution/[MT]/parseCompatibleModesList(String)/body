{
  ArrayList<String> modesList=new ArrayList<String>();
  if (unparsedModes == null || unparsedModes.isEmpty())   return modesList;
  String[] splitStr=unparsedModes.split(",");
  for (  String mode : splitStr)   modesList.add(mode.trim());
  return modesList;
}
