{
  int x=e.getX();
  int y=e.getY();
  if ((x > menuLeft) && (x < menuRight)) {
    popup.show(this,x,y);
  }
 else {
    for (int i=0; i < fileCount; i++) {
      if ((x > tabLeft[i]) && (x < tabRight[i])) {
        showTab(i);
      }
    }
  }
}
