{
  if (which == DISABLE_DEPTH_SORT) {
    flush();
  }
 else   if (which == DISABLE_DEPTH_TEST) {
    if (zbuffer != null) {
      Arrays.fill(zbuffer,Float.MAX_VALUE);
    }
  }
  super.hint(which);
}
