{
  this.parent=parent;
  gpipeline=null;
  GSVideo.init();
  parent.registerDispose(this);
  setEventHandlerObject(parent);
  pipeline=pstr;
  streamType=type;
  bufWidth=bufHeight=bufSize=0;
  pipelineReady=false;
}
