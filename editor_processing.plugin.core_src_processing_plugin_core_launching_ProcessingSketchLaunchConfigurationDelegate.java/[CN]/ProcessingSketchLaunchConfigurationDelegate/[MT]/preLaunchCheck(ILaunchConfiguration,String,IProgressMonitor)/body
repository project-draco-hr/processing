{
  if (mode.equals(ILaunchManager.RUN_MODE)) {
    if (configuration.getAttribute("wasLastBuildSuccessful",false))     return true;
    ProcessingLog.logInfo("Aborting launch -- Sketch has unresolved build problems.");
  }
  return false;
}
