{
  float tx=mx - width / 2.0f;
  float ty=my - height / 2.0f;
  float c2=(tx * tx + ty * ty - h * h - l * l) / (2 * h * l);
  float s2=sqrt(abs(1 - c2 * c2));
  l_0=-atan2(s2,c2);
  h_0=atan2(ty,tx) - atan2(l * s2,h + l * c2);
}
