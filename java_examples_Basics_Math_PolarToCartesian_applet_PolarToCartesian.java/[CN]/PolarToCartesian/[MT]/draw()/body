{
  background(0);
  translate(width / 2,height / 2);
  float x=r * cos(theta);
  float y=r * sin(theta);
  ellipseMode(CENTER);
  noStroke();
  fill(200);
  ellipse(x,y,16,16);
  theta_vel+=theta_acc;
  theta+=theta_vel;
}
