{
  background(0,0,26);
  translate(width / 2,height / 2);
  a+=0.01f;
  for (int i=0; i < num; i++) {
    pushMatrix();
    fill(colors[i]);
    rotateY(a + offset * i);
    rotateX(a / 2 + offset * i);
    box(200);
    popMatrix();
  }
}
