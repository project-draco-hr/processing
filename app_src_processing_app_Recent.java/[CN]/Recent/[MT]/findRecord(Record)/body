{
  int index=records.indexOf(rec);
  if (index != -1) {
    return index;
  }
  index=0;
  for (  Record r : records) {
    if (r.equals(rec)) {
      return index;
    }
    index++;
  }
  return -1;
}
