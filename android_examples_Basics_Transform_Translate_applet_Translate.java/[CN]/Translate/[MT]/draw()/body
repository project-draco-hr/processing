{
  background(102);
  x=x + 0.8f;
  if (x > width + size) {
    x=-size;
  }
  translate(x,height / 2 - size / 2);
  fill(255);
  rect(-size / 2,-size / 2,size,size);
  translate(x,size);
  fill(0);
  rect(-size / 2,-size / 2,size,size);
}
