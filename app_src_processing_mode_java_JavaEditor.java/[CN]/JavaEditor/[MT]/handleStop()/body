{
  toolbar.activate(Toolbar.STOP);
  try {
    jmode.handleStop();
  }
 catch (  Exception e) {
    statusError(e);
  }
  toolbar.deactivate(Toolbar.RUN);
  toolbar.deactivate(Toolbar.STOP);
  toFront();
}
