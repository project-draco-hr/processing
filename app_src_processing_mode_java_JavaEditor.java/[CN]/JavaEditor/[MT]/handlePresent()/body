{
  toolbar.activate(JavaToolbar.RUN);
  new Thread(new Runnable(){
    public void run(){
      prepareRun();
      try {
        jmode.handlePresent(sketch,JavaEditor.this);
      }
 catch (      Exception e) {
        statusError(e);
      }
    }
  }
).start();
}
