{
  toolbar.activate(JavaToolbar.RUN);
  new Thread(new Runnable(){
    public void run(){
      prepareRun();
      try {
        jmode.handleRun(sketch,JavaEditor.this);
      }
 catch (      Exception e) {
        statusError(e);
      }
    }
  }
).start();
}
