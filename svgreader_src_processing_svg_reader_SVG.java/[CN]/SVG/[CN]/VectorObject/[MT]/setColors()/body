{
  p.colorMode(PConstants.RGB,255,255,255,255);
  if (strokeColor != transValue)   p.stroke(strokeColor);
 else   p.noStroke();
  if (fillColor != transValue)   p.fill(fillColor);
 else   p.noFill();
  p.strokeWeight(strokeWeight);
}
