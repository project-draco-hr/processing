{
  if (!styleOverride)   setColors();
  if (hasTransform) {
    p.pushMatrix();
    p.translate(translateX,translateY);
    p.rotate(rotation);
  }
  draw();
  if (hasTransform) {
    p.popMatrix();
  }
}
