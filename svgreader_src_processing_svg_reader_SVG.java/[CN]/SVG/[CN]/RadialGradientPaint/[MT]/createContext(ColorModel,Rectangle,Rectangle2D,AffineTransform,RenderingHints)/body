{
  Point2D transformedPoint=xform.transform(new Point2D.Float(cx,cy),null);
  return new RadialGradientContext((float)transformedPoint.getX(),(float)transformedPoint.getY(),radius,offset,color,count,opacity);
}
