{
  int w, h;
  float l=PApplet.sqrt(size);
  for (w=2; w < l; w*=2)   ;
  int n0=w * w;
  int n1=w * w / 2;
  if (PApplet.abs(n0 - size) < PApplet.abs(n1 - size))   h=w;
 else   h=w / 2;
  width=w;
  height=h;
}
