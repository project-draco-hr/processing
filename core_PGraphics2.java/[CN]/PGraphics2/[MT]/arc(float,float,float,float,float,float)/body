{
  if (arcMode == CORNERS) {
    w-=x;
    h-=y;
  }
 else   if (arcMode == CENTER_RADIUS) {
    x-=w;
    y-=h;
    w*=2;
    h*=2;
  }
 else   if (arcMode == CENTER) {
    x-=w;
    y-=h;
  }
  arc.setArc(x,y,w,h,start,stop - start,Arc2D.PIE);
  draw_shape(arc);
}
