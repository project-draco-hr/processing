{
switch (shape) {
case LINE_STRIP:
    stroke_shape(gpath);
  break;
case LINE_LOOP:
gpath.closePath();
stroke_shape(gpath);
break;
case POLYGON:
gpath.closePath();
draw_shape(gpath);
break;
}
shape=0;
}
