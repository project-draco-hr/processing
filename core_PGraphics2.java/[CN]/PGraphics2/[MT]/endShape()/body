{
  shape=0;
switch (shape) {
case LINE_STRIP:
    stroke_shape(gpath);
  break;
case LINE_LOOP:
gpath.closePath();
stroke_shape(gpath);
break;
case POLYGON:
case CONCAVE_POLYGON:
case CONVEX_POLYGON:
gpath.closePath();
draw_shape(gpath);
break;
}
}
