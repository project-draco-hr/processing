{
  text=text.trim();
  name="";
  Pattern p=Pattern.compile("\\[(.*?)\\]\\((.*?)\\)");
  Matcher m=p.matcher(text);
  if (m.find()) {
    name=m.group(1);
    url=m.group(2);
  }
 else {
    name=text;
  }
}
