{
  if (matrixMode == PROJECTION) {
    if (!projectionUpdated) {
      getProjectionMatrix();
    }
    projection.print();
  }
 else {
    if (!modelviewUpdated) {
      getModelviewMatrix();
    }
    modelview.print();
  }
}
