{
  leftColor=-0.002f * x / 2 + 0.06f;
  rightColor=0.002f * x / 2 + 0.06f;
  gx=x / 2;
  gy=100 - x / 2;
  if (gx < 10) {
    gx=10;
  }
 else   if (gx > 90) {
    gx=90;
  }
  if (gy > 90) {
    gy=90;
  }
 else   if (gy < 10) {
    gy=10;
  }
}
