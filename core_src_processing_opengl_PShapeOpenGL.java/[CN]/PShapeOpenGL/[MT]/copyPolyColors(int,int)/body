{
  tessGeo.readyPolyColors();
  pgl.bindBuffer(PGL.ARRAY_BUFFER,glPolyColor);
  pgl.bufferSubData(PGL.ARRAY_BUFFER,offset * PGL.SIZEOF_INT,size * PGL.SIZEOF_INT,tessGeo.polyColors);
  pgl.bindBuffer(PGL.ARRAY_BUFFER,0);
}
