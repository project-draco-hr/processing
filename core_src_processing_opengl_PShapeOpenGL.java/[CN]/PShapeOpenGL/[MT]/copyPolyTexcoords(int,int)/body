{
  tessGeo.readyPolyTexcoords();
  pgl.bindBuffer(PGL.ARRAY_BUFFER,glPolyTexcoord);
  pgl.bufferSubData(PGL.ARRAY_BUFFER,2 * offset * PGL.SIZEOF_FLOAT,2 * size * PGL.SIZEOF_FLOAT,tessGeo.polyTexcoords);
  pgl.bindBuffer(PGL.ARRAY_BUFFER,0);
}
