{
  tessGeo.readyPointColors();
  pgl.bindBuffer(PGL.ARRAY_BUFFER,glPointColor);
  pgl.bufferSubData(PGL.ARRAY_BUFFER,offset * PGL.SIZEOF_INT,size * PGL.SIZEOF_INT,tessGeo.pointColors);
  pgl.bindBuffer(PGL.ARRAY_BUFFER,0);
}
