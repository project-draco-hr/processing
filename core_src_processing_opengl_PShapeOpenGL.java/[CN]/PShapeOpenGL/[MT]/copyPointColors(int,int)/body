{
  pgl.glBindBuffer(PGL.GL_ARRAY_BUFFER,glPointColor);
  pgl.glBufferSubData(PGL.GL_ARRAY_BUFFER,offset * PGL.SIZEOF_INT,size * PGL.SIZEOF_INT,IntBuffer.wrap(tessGeo.pointColors,offset,size));
  pgl.glBindBuffer(PGL.GL_ARRAY_BUFFER,0);
}
