{
  tessGeo.readyPolyNormals();
  pgl.bindBuffer(PGL.ARRAY_BUFFER,glPolyNormal);
  pgl.bufferSubData(PGL.ARRAY_BUFFER,3 * offset * PGL.SIZEOF_FLOAT,3 * size * PGL.SIZEOF_FLOAT,tessGeo.polyNormals);
  pgl.bindBuffer(PGL.ARRAY_BUFFER,0);
}
