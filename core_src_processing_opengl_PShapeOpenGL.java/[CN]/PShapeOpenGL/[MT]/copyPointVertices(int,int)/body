{
  tessGeo.readyPointVertices();
  pgl.bindBuffer(PGL.ARRAY_BUFFER,glPointVertex);
  pgl.bufferSubData(PGL.ARRAY_BUFFER,4 * offset * PGL.SIZEOF_FLOAT,4 * size * PGL.SIZEOF_FLOAT,tessGeo.pointVertices);
  pgl.bindBuffer(PGL.ARRAY_BUFFER,0);
}
