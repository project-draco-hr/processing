{
  tessGeo.readyLineColors();
  pgl.bindBuffer(PGL.ARRAY_BUFFER,glLineColor);
  pgl.bufferSubData(PGL.ARRAY_BUFFER,offset * PGL.SIZEOF_INT,size * PGL.SIZEOF_INT,tessGeo.lineColors);
  pgl.bindBuffer(PGL.ARRAY_BUFFER,0);
}
