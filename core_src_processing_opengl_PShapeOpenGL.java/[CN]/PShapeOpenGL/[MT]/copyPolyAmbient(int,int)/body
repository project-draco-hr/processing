{
  tessGeo.readyPolyAmbient();
  pgl.bindBuffer(PGL.ARRAY_BUFFER,glPolyAmbient);
  pgl.bufferSubData(PGL.ARRAY_BUFFER,offset * PGL.SIZEOF_INT,size * PGL.SIZEOF_INT,tessGeo.polyAmbient);
  pgl.bindBuffer(PGL.ARRAY_BUFFER,0);
}
