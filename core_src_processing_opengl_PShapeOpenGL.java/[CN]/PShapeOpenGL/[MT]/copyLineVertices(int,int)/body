{
  tessGeo.readyLineVertices();
  pgl.bindBuffer(PGL.ARRAY_BUFFER,glLineVertex);
  pgl.bufferSubData(PGL.ARRAY_BUFFER,4 * offset * PGL.SIZEOF_FLOAT,4 * size * PGL.SIZEOF_FLOAT,tessGeo.lineVertices);
  pgl.bindBuffer(PGL.ARRAY_BUFFER,0);
}
