{
  PGraphicsOpenGL g=pg.get();
  if (g == null)   return;
  if (savedCurveDetail != curveDetail) {
    g.curveDetail(savedCurveDetail);
  }
  if (savedCurveTightness != curveTightness) {
    g.curveTightness(savedCurveTightness);
  }
}
