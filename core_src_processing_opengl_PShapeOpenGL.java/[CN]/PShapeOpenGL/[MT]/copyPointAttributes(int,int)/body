{
  pgl.glBindBuffer(PGL.GL_ARRAY_BUFFER,glPointAttrib);
  pgl.glBufferSubData(PGL.GL_ARRAY_BUFFER,2 * offset * PGL.SIZEOF_FLOAT,2 * size * PGL.SIZEOF_FLOAT,FloatBuffer.wrap(tessGeo.pointAttribs,2 * offset,2 * size));
  pgl.glBindBuffer(PGL.GL_ARRAY_BUFFER,0);
}
