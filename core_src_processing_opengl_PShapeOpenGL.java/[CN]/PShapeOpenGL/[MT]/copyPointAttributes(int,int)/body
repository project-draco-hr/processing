{
  tessGeo.readyPointAttribs();
  pgl.bindBuffer(PGL.ARRAY_BUFFER,glPointAttrib);
  pgl.bufferSubData(PGL.ARRAY_BUFFER,2 * offset * PGL.SIZEOF_FLOAT,2 * size * PGL.SIZEOF_FLOAT,tessGeo.pointAttribs);
  pgl.bindBuffer(PGL.ARRAY_BUFFER,0);
}
