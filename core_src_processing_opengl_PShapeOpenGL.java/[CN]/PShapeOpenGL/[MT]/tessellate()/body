{
  if (root == this && parent == null) {
    if (tessGeo == null) {
      tessGeo=PGraphicsOpenGL.newTessGeometry(pg,attribs,PGraphicsOpenGL.RETAINED);
    }
    tessGeo.clear();
    tessellateImpl();
    tessGeo.trim();
  }
}
