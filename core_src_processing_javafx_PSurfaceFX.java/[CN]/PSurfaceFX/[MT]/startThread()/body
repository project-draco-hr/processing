{
  if (timer == null) {
    timer=new AnimationTimer(){
      @Override public void handle(      long now){
        sketch.handleDraw();
        if (sketch.exitCalled()) {
          Platform.exit();
        }
      }
    }
;
    timer.start();
  }
}
