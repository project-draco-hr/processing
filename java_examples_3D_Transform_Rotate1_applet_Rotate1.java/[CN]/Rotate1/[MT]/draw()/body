{
  background(0);
  a+=0.005f;
  if (a > TWO_PI) {
    a=0.0f;
  }
  translate(width / 2,height / 2);
  rotateX(a);
  rotateY(a * 2.0f);
  rect(-rSize,-rSize,rSize * 2,rSize * 2);
  rotateX(a * 1.001f);
  rotateY(a * 2.002f);
  rect(-rSize,-rSize,rSize * 2,rSize * 2);
}
