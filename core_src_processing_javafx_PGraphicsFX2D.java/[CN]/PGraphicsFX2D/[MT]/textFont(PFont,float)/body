{
  if (which != null) {
    if (size <= 0) {
      System.err.println("textFont with size " + size + " ignored: "+ "the text size must be larger than zero");
      size=textSize;
    }
    textFontImpl(which,size);
  }
 else {
    throw new RuntimeException(ERROR_TEXTFONT_NULL_PFONT);
  }
}
