{
  pushMatrix();
  float arrowsize=4;
  translate(x,y);
  stroke(255);
  rotate(heading2D());
  float len=magnitude() * scayl;
  line(0,0,len,0);
  line(len,0,len - arrowsize,+arrowsize / 2);
  line(len,0,len - arrowsize,-arrowsize / 2);
  popMatrix();
}
