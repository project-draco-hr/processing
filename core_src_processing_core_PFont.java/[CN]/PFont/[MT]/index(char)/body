{
  if (lazy) {
    int index=indexActual(c);
    if (index != -1) {
      return index;
    }
    if (font.canDisplay(c)) {
      createGlyph(c);
      return indexActual(c);
    }
 else {
      return -1;
    }
  }
 else {
    return indexActual(c);
  }
}
