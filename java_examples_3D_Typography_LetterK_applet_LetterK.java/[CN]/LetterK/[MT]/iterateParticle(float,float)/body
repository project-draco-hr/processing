{
  pfx=fkx;
  pfy=fky;
  pa2=pfx * pfx + pfy * pfy;
  if (pa2 < 0.0000001f) {
    return;
  }
  pax=pfx / pMass;
  pay=pfy / pMass;
  pvx+=pax;
  pvy+=pay;
  pv2=pvx * pvx + pvy * pvy;
  if (pv2 < 0.0000001f) {
    return;
  }
  pvx*=(1.0f - pDrag);
  pvy*=(1.0f - pDrag);
  px+=pvx;
  py+=pvy;
}
