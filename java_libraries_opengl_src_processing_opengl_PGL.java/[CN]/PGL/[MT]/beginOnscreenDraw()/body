{
  if (drawable != null) {
    drawable.setRealized(pg.parent.isDisplayable());
    if (pg.parent.isDisplayable()) {
      drawable.setRealized(true);
      return true;
    }
 else {
      return false;
    }
  }
  return false;
}
