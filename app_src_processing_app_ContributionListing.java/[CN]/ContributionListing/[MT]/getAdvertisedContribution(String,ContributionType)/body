{
  for (  ContributionInfo advertised : advertisedContributions) {
    if (advertised.getType() == contributionType && advertised.name.equals(contributionName)) {
      return advertised;
    }
  }
  return null;
}
