{
  for (  Contribution advertised : advertisedContributions) {
    if (advertised.getType() == contributionType && advertised.getName().equals(contributionName)) {
      return advertised;
    }
  }
  return null;
}
