{
  if (ContributionXmlParser.LIBRARY_TAG.equals(qName) || ContributionXmlParser.LIBRARY_COMPILATION_TAG.equals(qName) || ContributionXmlParser.TOOL_TAG.equals(qName)) {
    contributions.add(currentInfo);
    currentInfo=null;
  }
}
