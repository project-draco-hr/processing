{
  if (path.startsWith("application" + File.separator)) {
    return new File(Base.getContentFile("modes" + File.separator + "java").getAbsolutePath() + File.separator + path);
  }
  return new File(folder,path);
}
