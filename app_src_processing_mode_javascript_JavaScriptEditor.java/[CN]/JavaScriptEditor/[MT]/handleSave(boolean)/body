{
  if (untitled) {
    return handleSaveAs();
  }
 else   if (immediately) {
    handleSave();
    statusEmpty();
    handleStartServer();
  }
 else {
    SwingUtilities.invokeLater(new Runnable(){
      public void run(){
        handleSave();
        statusEmpty();
        handleStartServer();
      }
    }
);
  }
  return true;
}
