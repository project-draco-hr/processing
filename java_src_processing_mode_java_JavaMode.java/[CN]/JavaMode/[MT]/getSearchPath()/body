{
  StringBuilder tehPath=new StringBuilder(System.getProperty("java.class.path"));
  tehPath.append(File.pathSeparatorChar + System.getProperty("java.home") + File.separator+ "lib"+ File.separator+ "rt.jar");
  return tehPath.toString();
}
