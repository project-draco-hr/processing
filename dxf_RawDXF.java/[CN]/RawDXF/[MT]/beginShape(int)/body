{
  shape=kind;
  if ((shape != LINES) && (shape != TRIANGLES)) {
    String err="RawDXF should only be used with beginRaw(), " + "as it only supports lines and triangles";
    throw new RuntimeException(err);
  }
  vertexCount=0;
}
