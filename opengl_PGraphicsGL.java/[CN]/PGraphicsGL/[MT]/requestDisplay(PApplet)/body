{
  if (!displayed) {
    canvas.setRenderingThread(Thread.currentThread());
    canvas.setNoAutoRedrawMode(true);
    canvas.requestFocus();
    displayed=true;
  }
  try {
    canvas.display();
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
