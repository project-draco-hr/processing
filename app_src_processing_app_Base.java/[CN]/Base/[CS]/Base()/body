{
  if (PApplet.platform == PConstants.MACOSX) {
    registerMacOS();
  }
  try {
    String path=Sketchbook.handleNewUntitled();
    handleOpen(path);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
