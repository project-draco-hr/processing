{
  int index=0;
  for (  Editor editor : editors) {
    if (editor.checkModified()) {
      storeSketchPath(editor,index);
      index++;
    }
 else {
      return false;
    }
  }
  return true;
}
