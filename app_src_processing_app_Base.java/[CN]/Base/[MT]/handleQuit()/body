{
  boolean canceled=false;
  for (int i=0; i < editorCount; i++) {
    if (!handleClose(editors[i])) {
      canceled=true;
      break;
    }
  }
  if (!canceled) {
    Base.cleanSketchbook();
    System.exit(0);
  }
  return !canceled;
}
