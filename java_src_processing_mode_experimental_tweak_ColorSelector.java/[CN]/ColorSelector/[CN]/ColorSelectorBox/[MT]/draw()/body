{
  image(backImg,0,0);
  stroke((lastY < 128) ? 0 : 255);
  pushMatrix();
  translate(lastX,lastY);
  ellipse(0,0,5,5);
  line(-8,0,-6,0);
  line(6,0,8,0);
  line(0,-8,0,-6);
  line(0,6,0,8);
  popMatrix();
}
