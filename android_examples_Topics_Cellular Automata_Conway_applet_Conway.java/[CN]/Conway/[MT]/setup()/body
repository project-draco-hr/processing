{
  size(640,200,P3D);
  frameRate(12);
  sx=width;
  sy=height;
  world=new int[sx][sy][2];
  stroke(255);
  for (int i=0; i < sx * sy * density; i++) {
    world[(int)random(sx)][(int)random(sy)][1]=1;
  }
}
