{
  try {
    IFolder data=project.getFolder("data");
    if (!data.exists())     data.create(IResource.NONE,true,null);
    return data;
  }
 catch (  Exception e) {
    ProcessingLog.logError("Data folder could not be created.",e);
    return null;
  }
}
