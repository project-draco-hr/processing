{
  File build=getBuildFolder();
  if (build != null) {
    File compile=new File(build,"compile");
    compile.mkdirs();
    if (compile.exists())     return compile;
  }
  ProcessingLog.logError("Could not create the temporary build (compile) folder.",null);
  return null;
}
