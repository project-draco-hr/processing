{
  try {
    IFolder code=project.getFolder("code");
    if (!code.exists())     code.create(IResource.NONE,true,null);
    return code;
  }
 catch (  Exception e) {
    ProcessingLog.logError("Code folder could not be created.",e);
    return null;
  }
}
