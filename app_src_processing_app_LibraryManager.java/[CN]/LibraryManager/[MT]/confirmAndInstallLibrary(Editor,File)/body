{
  this.editor=editor;
  String prompt="Install libraries from " + libFile.getName() + "?  ";
  int result=JOptionPane.showConfirmDialog(this.editor,prompt,"Close",JOptionPane.YES_NO_OPTION,JOptionPane.QUESTION_MESSAGE);
  if (result == 0) {
    return installLibrary(libFile);
  }
  return 0;
}
