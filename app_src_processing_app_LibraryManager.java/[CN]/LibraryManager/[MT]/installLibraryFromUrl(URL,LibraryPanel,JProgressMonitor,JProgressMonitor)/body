{
  File libDest=getTemporaryFile(url);
  FileDownloader downloader=new FileDownloader(url,libDest,downloadProgressMonitor);
  downloader.setPostOperation(new LibraryInstaller(downloader,libPanel,installProgressMonitor));
  new Thread(downloader).start();
}
