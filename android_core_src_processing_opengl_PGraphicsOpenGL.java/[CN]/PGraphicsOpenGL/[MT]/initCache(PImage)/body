{
  Texture tex=(Texture)img.getCache(pgPrimary);
  if (tex == null || tex.contextIsOutdated()) {
    tex=addTexture(img);
    if (img.pixels != null) {
      tex.set(img.pixels);
    }
  }
  return tex;
}
