{
  if (stroke) {
    if (in.edgeCount == 0) {
      return;
    }
    if (is3D) {
      tessellateEdges3D();
    }
 else     if (is2D) {
      beginNoTex();
      tessellateEdges2D();
      endNoTex();
    }
  }
}
