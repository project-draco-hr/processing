{
  String apath=folder.getAbsolutePath();
  if (apath.startsWith(PdeSketchbook.examplesPath) || apath.startsWith(PdeSketchbook.librariesPath)) {
    return true;
  }
 else {
    for (int i=0; i < codeCount; i++) {
      if (code[i].modified && !code[i].file.canWrite()) {
        return true;
      }
    }
  }
  return false;
}
