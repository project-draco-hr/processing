{
  if (shapeEnded) {
    if (family == GROUP) {
      updateTessellation();
    }
    checkMatrix(ncoords);
    calcTransform(type,ncoords,args);
    if (tessellated) {
      applyMatrixImpl(transform);
    }
  }
}
