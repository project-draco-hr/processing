{
  if (matrix != null && applyMatrix) {
    return true;
  }
  if (family == GROUP) {
    for (int i=0; i < childCount; i++) {
      PShape3D child=(PShape3D)children[i];
      if (child.hasMatrix()) {
        return true;
      }
    }
  }
  return false;
}
