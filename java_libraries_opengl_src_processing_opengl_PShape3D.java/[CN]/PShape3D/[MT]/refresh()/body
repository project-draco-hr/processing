{
  if (root != this)   return;
  loadVertices();
  updateVertices();
  loadColors();
  updateColors();
  loadNormals();
  updateNormals();
  for (int i=0; i < numTexBuffers; i++) {
    loadTexcoords();
    updateTexcoords();
  }
}
