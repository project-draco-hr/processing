{
  if (family == GROUP) {
    for (int i=0; i < childCount; i++) {
      PShape3D child=(PShape3D)children[i];
      child.ambient(x,y,z);
    }
  }
 else {
    colorCalc(x,y,z);
    ambientFromCalc();
  }
}
