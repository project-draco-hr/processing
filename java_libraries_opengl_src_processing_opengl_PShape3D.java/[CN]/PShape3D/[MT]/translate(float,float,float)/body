{
  if (family == GROUP) {
    super.translate(tx,ty,tz);
  }
 else {
    checkMatrix(3);
    matrix.translate(tx,ty,tz);
    tess.applyMatrix((PMatrix3D)matrix);
    transformed=true;
    matrix=null;
  }
}
