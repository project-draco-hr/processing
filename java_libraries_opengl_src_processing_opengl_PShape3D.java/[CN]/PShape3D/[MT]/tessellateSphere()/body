{
  float r=params[0];
  int nu=pg.sphereDetailU;
  int nv=pg.sphereDetailV;
  int[] indices=in.addSphere(r,nu,nv,fill,fillColor,stroke,strokeColor,strokeWeight,ambientColor,specularColor,emissiveColor,shininess);
  in.initTessMaps();
  tessellator.tessellateTriangles(indices);
}
