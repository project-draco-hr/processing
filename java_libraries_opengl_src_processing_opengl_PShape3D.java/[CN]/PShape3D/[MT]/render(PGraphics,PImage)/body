{
  if (root == null) {
    return;
  }
  if (0 < vertexCount && 0 < indexCount) {
    renderFill(texture);
  }
  if (isStroked) {
    renderStroke();
  }
  if (hasPoints) {
    renderPoints();
  }
}
