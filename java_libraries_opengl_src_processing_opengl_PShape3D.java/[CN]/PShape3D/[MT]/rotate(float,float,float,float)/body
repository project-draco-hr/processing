{
  if (family == GROUP) {
    super.rotate(angle,v0,v1,v2);
  }
 else {
    checkMatrix(3);
    matrix.rotate(angle,v0,v1,v2);
    tess.applyMatrix((PMatrix3D)matrix);
    transformed=true;
    matrix=null;
  }
}
