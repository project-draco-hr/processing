{
  if (0 <= i && i < inVertexCount) {
    inVertices[3 * i + 0]=x;
    inVertices[3 * i + 1]=y;
    inVertices[3 * i + 2]=z;
    modified=true;
  }
 else {
    System.err.println("Wrong index");
  }
}
