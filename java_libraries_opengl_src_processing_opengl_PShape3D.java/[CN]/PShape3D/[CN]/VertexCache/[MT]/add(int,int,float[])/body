{
  if (size == 0) {
    offset=newOffset;
  }
  if (data.length / ncoords == size + newSize) {
    expand(size + newSize);
  }
  PApplet.arrayCopy(newData,0,data,ncoords * size,ncoords * newSize);
  size+=newSize;
}
