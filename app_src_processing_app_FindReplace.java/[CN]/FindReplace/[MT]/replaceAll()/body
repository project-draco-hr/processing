{
  editor.setSelection(0,0);
  boolean foundAtLeastOne=false;
  while (true) {
    if (find(false,false)) {
      foundAtLeastOne=true;
      replace();
    }
 else {
      break;
    }
  }
  if (!foundAtLeastOne) {
    Toolkit.beep();
  }
  setFound(false);
}
