{
  if (unwarped()) {
    float x=matrix.multX(centerX,centerY);
    float y=matrix.multY(centerX,centerY);
    centerX=(int)x;
    centerY=(int)y;
  }
  if (fill)   flat_circle_fill(centerX,centerY,radius);
  if (stroke)   flat_circle_stroke(centerX,centerY,radius);
}
