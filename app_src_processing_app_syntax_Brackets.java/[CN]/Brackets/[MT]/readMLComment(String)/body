{
  final int len=text.length();
  for (pos++; pos < len; pos++) {
    final char c=text.charAt(pos);
    if (c == '*' && (pos < len - 1)) {
      pos++;
      final char d=text.charAt(pos);
      if (d == '/') {
        return;
      }
    }
  }
}
