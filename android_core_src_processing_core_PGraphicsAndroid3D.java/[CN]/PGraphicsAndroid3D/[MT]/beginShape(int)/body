{
  shape=kind;
  if (hints[ENABLE_DEPTH_SORT]) {
    shapeFirst=vertexCount;
    shapeLast=0;
  }
 else {
    vertexCount=0;
    lineCount=0;
    triangleCount=0;
  }
  textureImages.clear();
  textureImagesPrev.clear();
  normalMode=AUTO;
}
