{
  if (matrixMode == PROJECTION) {
    if (!projectionUpdated) {
      getProjectionMatrix();
    }
    return projection.get();
  }
 else {
    if (!modelviewUpdated) {
      getModelviewMatrix();
    }
    return modelview.get();
  }
}
