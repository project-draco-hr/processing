{
  return MAX_TESS_VERTICES <= fillVertexCount || MAX_TESS_VERTICES <= lineVertexCount || MAX_TESS_VERTICES <= pointVertexCount || MAX_TESS_INDICES <= fillIndexCount || MAX_TESS_INDICES <= fillIndexCount || MAX_TESS_INDICES <= fillIndexCount;
}
