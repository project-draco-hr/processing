{
  pgl.glBindBuffer(PGL.GL_ARRAY_BUFFER,glLineColor);
  pgl.glBufferSubData(PGL.GL_ARRAY_BUFFER,offset * PGL.SIZEOF_INT,size * PGL.SIZEOF_INT,IntBuffer.wrap(tessGeo.lineColors,offset,size));
  pgl.glBindBuffer(PGL.GL_ARRAY_BUFFER,0);
}
