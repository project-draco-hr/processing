{
  if (!stroke) {
    markForTessellation();
    stroke=true;
  }
  updateStrokeColor(calcColor);
  if (is2D() && image != null && parent != null) {
    ((PShapeOpenGL)parent).strokedTexture(true);
  }
}
