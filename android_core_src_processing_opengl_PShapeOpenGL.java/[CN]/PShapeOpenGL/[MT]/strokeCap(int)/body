{
  if (family == GROUP) {
    for (int i=0; i < childCount; i++) {
      PShapeOpenGL child=(PShapeOpenGL)children[i];
      child.strokeCap(cap);
    }
  }
 else {
    if (is2D() && strokeCap != cap) {
      markForTessellation();
    }
    strokeCap=cap;
  }
}
