{
  pgl.bindBuffer(PGL.ARRAY_BUFFER,glPolySpecular);
  pgl.bufferSubData(PGL.ARRAY_BUFFER,offset * PGL.SIZEOF_INT,size * PGL.SIZEOF_INT,IntBuffer.wrap(tessGeo.polySpecular,offset,size));
  pgl.bindBuffer(PGL.ARRAY_BUFFER,0);
}
