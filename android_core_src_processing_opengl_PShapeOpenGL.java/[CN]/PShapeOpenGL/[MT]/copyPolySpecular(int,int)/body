{
  pgl.glBindBuffer(PGL.GL_ARRAY_BUFFER,glPolySpecular);
  pgl.glBufferSubData(PGL.GL_ARRAY_BUFFER,offset * PGL.SIZEOF_INT,size * PGL.SIZEOF_INT,IntBuffer.wrap(tessGeo.polySpecular,offset,size));
  pgl.glBindBuffer(PGL.GL_ARRAY_BUFFER,0);
}
