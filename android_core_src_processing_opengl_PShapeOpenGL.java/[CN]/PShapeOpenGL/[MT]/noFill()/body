{
  if (family == GROUP) {
    for (int i=0; i < childCount; i++) {
      PShapeOpenGL child=(PShapeOpenGL)children[i];
      child.noFill();
    }
  }
 else {
    fill=false;
    updateFillColor(0x0);
  }
}
