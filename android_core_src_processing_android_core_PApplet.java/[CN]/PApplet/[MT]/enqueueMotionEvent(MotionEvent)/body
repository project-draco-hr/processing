{
synchronized (motionEventQueue) {
    if (motionEventCount == motionEventQueue.length) {
      motionEventQueue=(MotionEvent[])PApplet.expand(motionEventQueue);
    }
    motionEventQueue[motionEventCount++]=e;
  }
}
