{
  background(75);
  float dx=(mouseX - width / 2) / 1000.0f;
  Vector3D wind=new Vector3D(dx,0,0);
  wind.display(width / 2,50,500);
  ps.add_force(wind);
  ps.run();
  for (int i=0; i < 2; i++) {
    ps.addParticle();
  }
}
