{
  if (flushTimer == null) {
    flushTimer=new Timer(250,new ActionListener(){
      public void actionPerformed(      ActionEvent evt){
        flush();
      }
    }
);
    flushTimer.start();
  }
}
