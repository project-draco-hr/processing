{
  try {
    if (downloadFile(url,dest,progressMonitor)) {
      libFile=dest;
    }
  }
 catch (  IOException e) {
    Base.showWarning("Trouble downloading file","An error occured while downloading the library:\n" + e.getMessage(),e);
  }
  if (post != null) {
    post.run();
  }
  progressMonitor.finished();
}
