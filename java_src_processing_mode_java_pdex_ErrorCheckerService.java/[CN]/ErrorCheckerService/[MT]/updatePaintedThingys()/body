{
  currentTab=editor.getSketch().getCurrentCodeIndex();
  if (currentTab != lastTab) {
    textModified.set(5);
    lastTab=currentTab;
    editor.getTextArea().repaint();
    editor.statusEmpty();
  }
}
