{
  for (int ptIdx=0; ptIdx < numPointers; ptIdx++) {
    motionX[ptIdx]=event.getX(ptIdx);
    motionY[ptIdx]=event.getY(ptIdx);
    motionPressure[ptIdx]=event.getPressure(ptIdx);
    mouseX[ptIdx]=(int)motionX[ptIdx];
    mouseY[ptIdx]=(int)motionY[ptIdx];
  }
}
