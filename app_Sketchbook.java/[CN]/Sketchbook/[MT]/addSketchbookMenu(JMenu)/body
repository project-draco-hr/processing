{
  try {
    JMenu smenu=new JMenu("Sketchbook");
    boolean sketches=addSketches(smenu,new File(getSketchbookPath()));
    menu.add(smenu);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
