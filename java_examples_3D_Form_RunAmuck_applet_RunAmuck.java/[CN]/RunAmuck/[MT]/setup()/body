{
  size(640,360,P3D);
  noStroke();
  for (int i=0; i < legs.length; i++) {
    legs[i]=new Legs(random(-10,10),random(-50,150),random(.5f,5),random(.5f,5),color(random(255),random(255),random(255)));
  }
}
