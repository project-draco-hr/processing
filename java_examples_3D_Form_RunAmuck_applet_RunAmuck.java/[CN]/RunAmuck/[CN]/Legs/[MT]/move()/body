{
  y=stepR * damping;
  x+=speedX;
  if (screenX(x,y,z) > width) {
    speedX*=-1;
  }
  if (screenX(x,y,z) < 0) {
    speedX*=-1;
  }
  speedZ=(stepRate * velocity);
  z+=speedZ;
  if (z > 400) {
    z=400;
    velocity*=-1;
  }
  if (z < -100) {
    z=-100;
    velocity*=-1;
  }
}
