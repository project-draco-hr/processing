{
  try {
    LineMarker m=findClosestMarker(y);
    if (m != null) {
      editor.getErrorChecker().scrollToErrorLine(m.getProblem());
    }
  }
 catch (  Exception ex) {
    ex.printStackTrace();
  }
}
