{
  this.pg=pg;
  if (glu == null) {
    glu=new GLU();
  }
  if (byteBuffer == null) {
    byteBuffer=allocateDirectByteBuffer(1);
  }
  if (intBuffer == null) {
    intBuffer=allocateDirectIntBuffer(1);
  }
  initialized=false;
}
