{
  if (initialized) {
    try {
      if (toolkit == AWT) {
        canvasAWT.display();
      }
 else       if (toolkit == NEWT) {
        animator.requestDisplay();
      }
    }
 catch (    GLException e) {
      Throwable tr=e.getCause();
      throw new RuntimeException(tr);
    }
  }
}
