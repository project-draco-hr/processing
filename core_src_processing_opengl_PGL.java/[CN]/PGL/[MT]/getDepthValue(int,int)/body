{
  PGraphicsOpenGL g=graphics.get();
  if (g == null)   return 0;
  if (depthBuffer == null) {
    depthBuffer=FloatBuffer.allocate(1);
  }
  depthBuffer.rewind();
  readPixels(scrX,g.height - scrY - 1,1,1,DEPTH_COMPONENT,FLOAT,depthBuffer);
  return depthBuffer.get(0);
}
