{
  Texture tex=new Texture(pg.parent);
  tex.init(backTex.getName(),GL.GL_TEXTURE_2D,backTex.format,backTex.getWidth(),backTex.getHeight(),backTex.minFilter,backTex.magFilter,backTex.wrapS,backTex.wrapT);
  tex.invertedY(true);
  tex.colorBufferOf(pg);
  pg.setCache(pg,tex);
  return tex;
}
