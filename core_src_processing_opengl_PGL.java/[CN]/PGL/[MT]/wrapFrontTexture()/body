{
  Texture tex=new Texture(pg.parent);
  tex.init(backTex.getName(),GL.GL_TEXTURE_2D,frontTex.format,frontTex.getWidth(),frontTex.getHeight(),frontTex.minFilter,frontTex.magFilter,frontTex.wrapS,frontTex.wrapT);
  tex.invertedY(true);
  tex.colorBufferOf(pg);
  return tex;
}
