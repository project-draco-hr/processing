{
  Texture tex=new Texture(pg.parent);
  tex.init(pg.width,pg.height,backTex.getName(),GL.GL_TEXTURE_2D,GL.GL_RGBA,frontTex.getWidth(),frontTex.getHeight(),frontTex.minFilter,frontTex.magFilter,frontTex.wrapS,frontTex.wrapT);
  tex.invertedY(true);
  tex.colorBufferOf(pg);
  return tex;
}
