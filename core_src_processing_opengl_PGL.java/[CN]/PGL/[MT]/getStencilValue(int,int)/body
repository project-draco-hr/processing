{
  if (stencilBuffer == null) {
    stencilBuffer=ByteBuffer.allocate(1);
  }
  readPixels(scrX,pg.height - scrY - 1,1,1,STENCIL_INDEX,GL.GL_UNSIGNED_BYTE,stencilBuffer);
  return stencilBuffer.get(0);
}
