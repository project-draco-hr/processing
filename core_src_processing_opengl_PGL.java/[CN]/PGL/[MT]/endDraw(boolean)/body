{
  if (!clear && isFBOBacked() && !isMultisampled()) {
    frontFBO.bind(gl);
    gl.glDisable(GL.GL_BLEND);
    drawTexture(GL.GL_TEXTURE_2D,backTex.getName(),backTex.getWidth(),backTex.getHeight(),0,0,pg.width,pg.height,0,0,pg.width,pg.height);
    backFBO.bind(gl);
  }
}
