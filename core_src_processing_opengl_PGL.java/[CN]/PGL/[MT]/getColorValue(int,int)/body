{
  PGraphicsOpenGL g=graphics.get();
  if (g == null)   return 0;
  if (colorBuffer == null) {
    colorBuffer=IntBuffer.allocate(1);
  }
  colorBuffer.rewind();
  readPixels(scrX,g.height - scrY - 1,1,1,RGBA,UNSIGNED_BYTE,colorBuffer);
  return colorBuffer.get();
}
