{
  if (intersect(sx1,sy1,sx2,sy2,dx1,dy1,dx2,dy2)) {
    blit_resize(this.get(sx1,sy1,sx2 - sx1,sy2 - sy1),0,0,sx2 - sx1 - 1,sy2 - sy1 - 1,pixels,width,height,dx1,dy1,dx2,dy2,REPLACE);
  }
 else {
    blit_resize(this,sx1,sy1,sx2,sy2,pixels,width,height,dx1,dy1,dx2,dy2,REPLACE);
  }
}
