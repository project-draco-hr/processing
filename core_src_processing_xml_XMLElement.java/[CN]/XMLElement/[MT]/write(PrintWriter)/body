{
  writer.println(XMLWriter.HEADER);
  XMLWriter xmlw=new XMLWriter(writer);
  try {
    xmlw.write(this,true);
    writer.flush();
    return true;
  }
 catch (  IOException e) {
    e.printStackTrace();
    return false;
  }
}
