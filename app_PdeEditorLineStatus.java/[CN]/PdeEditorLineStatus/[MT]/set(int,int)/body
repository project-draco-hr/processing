{
  if ((newStart == start) && (newStop == stop))   return;
  start=newStart;
  stop=newStop;
  if (start == stop) {
    text=String.valueOf(start + 1);
  }
 else {
    text=(start + 1) + " - " + (stop + 1);
  }
  repaint();
}
