{
  if (!looping) {
    redraw=true;
    if (thread != null) {
      if (CRUSTY_THREADS) {
        thread.interrupt();
      }
 else {
synchronized (blocker) {
          blocker.notifyAll();
        }
      }
    }
  }
}
