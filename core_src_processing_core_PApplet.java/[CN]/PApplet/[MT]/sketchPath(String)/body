{
  if (sketchPath == null) {
    throw new RuntimeException("Applet not inited properly, " + "the sketch path could not be determined.");
  }
  if (new File(where).isAbsolute())   return where;
  return sketchPath + File.separator + where;
}
