{
  boolean changed=false;
  if (g == null) {
    g=PApplet.createGraphics(w,h,JAVA2D,null,this);
  }
 else   if (w != g.width || h != g.height) {
    g.resize(w,h);
    changed=true;
  }
  width=w;
  height=h;
  if (changed)   redraw();
}
