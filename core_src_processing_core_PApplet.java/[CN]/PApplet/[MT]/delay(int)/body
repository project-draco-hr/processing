{
  if (frameCount == 0)   return;
  if (napTime > 0) {
    try {
      if (CRUSTY_THREADS) {
        Thread.sleep(napTime);
      }
 else {
        wait(napTime);
      }
    }
 catch (    InterruptedException e) {
    }
  }
}
