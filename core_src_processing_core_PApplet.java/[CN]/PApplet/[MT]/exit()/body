{
  if (thread == null) {
    if ((leechErr == null) && !online) {
      System.exit(0);
    }
  }
 else {
    finished=true;
    exit=true;
    if (!looping) {
      stop();
      if ((leechErr == null) && !online) {
        System.exit(0);
      }
    }
  }
}
