{
  Runnable doConstruct=new Runnable(){
    public void run(){
      try {
        setValue(construct());
      }
  finally {
        workerVar.clear();
      }
    }
  }
;
  Thread t=new Thread(doConstruct);
  workerVar=new WorkerVar(t);
}
