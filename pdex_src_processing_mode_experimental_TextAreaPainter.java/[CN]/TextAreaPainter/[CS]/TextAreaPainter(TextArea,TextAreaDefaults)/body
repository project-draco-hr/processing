{
  super(textArea,defaults);
  ta=textArea;
  addMouseListener(new MouseAdapter(){
    public void mouseClicked(    MouseEvent evt){
      if (evt.getButton() == MouseEvent.BUTTON1) {
        if (evt.isControlDown() || evt.isMetaDown())         handleCtrlClick(evt);
      }
    }
  }
);
}
