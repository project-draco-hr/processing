{
  String path=buildFolderPath + File.separator + name.replace('.','/');
  File searchResource=new File(path,name);
  if (searchResource.exists()) {
    try {
      return searchResource.toURL();
    }
 catch (    MalformedURLException mfe) {
    }
  }
  return null;
}
