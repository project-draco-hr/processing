{
  System.setOut(systemOut);
  System.setErr(systemErr);
  consoleOut.close();
  consoleErr.close();
  try {
    stdoutFile.close();
    stderrFile.close();
  }
 catch (  IOException e) {
    e.printStackTrace(systemOut);
  }
  outFile.delete();
  errFile.delete();
  tempFolder.delete();
}
