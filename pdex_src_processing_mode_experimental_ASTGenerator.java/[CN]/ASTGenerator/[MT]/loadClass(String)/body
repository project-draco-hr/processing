{
  try {
    Class<?> probableClass=Class.forName(className,false,errorCheckerService.classLoader);
    return new ClassMember(probableClass);
  }
 catch (  ClassNotFoundException e) {
    e.printStackTrace();
    System.out.println("Couldn't load " + className);
  }
  return null;
}
