{
  if (node instanceof SimpleName) {
    SimpleName sn=(SimpleName)node;
    System.out.println("Visiting: " + getNodeAsString(node));
    if (sn.toString().equals(name)) {
      if (findDeclaration(sn).equals(decl))       return true;
    }
  }
  return false;
}
