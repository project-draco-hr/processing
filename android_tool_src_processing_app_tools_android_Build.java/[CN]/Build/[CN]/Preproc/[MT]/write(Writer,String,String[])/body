{
  if (sketchSize != null) {
    int start=program.indexOf(sketchSize);
    program=program.substring(0,start) + program.substring(start + sketchSize.length());
  }
  return super.write(out,program,codeFolderPackages);
}
