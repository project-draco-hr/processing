{
  pgl.getGL(drawable);
  sketch.handleDraw();
  if (sketch.frameCount == 1) {
    requestFocus();
  }
  if (sketch.exitCalled()) {
    System.out.println("exit");
    animator.stop();
    sketch.dispose();
    sketch.exitActual();
  }
}
