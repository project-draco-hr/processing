{
  if (animator.isAnimating()) {
    if (!presentMode) {
      sketch.width=width;
      sketch.height=height;
      graphics.setSize(width,height);
    }
    sketchWidth=width;
    sketchHeight=height;
  }
}
