{
  if (width == sketch.width && height == sketch.height) {
    return;
  }
  if (!presentMode) {
    sketch.setSize(width,height);
    sketchWidth=width;
    sketchHeight=height;
    graphics.setSize(width,height);
  }
}
