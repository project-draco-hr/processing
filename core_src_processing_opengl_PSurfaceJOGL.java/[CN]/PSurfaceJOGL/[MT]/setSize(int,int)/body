{
  if (width == sketch.width && height == sketch.height) {
    return;
  }
  if (!presentMode) {
    sketch.width=width;
    sketch.height=height;
    sketchWidth=width;
    sketchHeight=height;
    graphics.setSize(width,height);
  }
}
