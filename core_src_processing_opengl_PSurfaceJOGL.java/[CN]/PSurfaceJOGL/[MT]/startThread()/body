{
  if (animator == null)   return;
  if (placedWindow) {
    window.setVisible(true);
    animator.start();
    requestedStart=false;
  }
 else {
    requestedStart=true;
    sketch.defaultSize=false;
  }
}
