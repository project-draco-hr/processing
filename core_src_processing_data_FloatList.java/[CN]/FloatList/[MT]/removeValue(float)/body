{
  if (Float.isNaN(value)) {
    for (int i=0; i < count; i++) {
      if (Float.isNaN(data[i])) {
        remove(i);
        return true;
      }
    }
  }
 else {
    int index=index(value);
    if (index != -1) {
      remove(index);
      return true;
    }
  }
  return false;
}
