{
  System.setErr(leechErr);
  String args[]=new String[2];
  args[0]="-d" + buildPath;
  args[1]=buildPath + File.separator + className+ ".java";
  boolean success=at.dms.kjc.Main.compile(args);
  System.setErr(PdeEditorConsole.consoleErr);
  return success;
}
