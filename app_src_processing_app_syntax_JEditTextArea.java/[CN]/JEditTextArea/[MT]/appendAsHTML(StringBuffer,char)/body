{
  if (c == '<') {
    buffer.append("&lt;");
  }
 else   if (c == '>') {
    buffer.append("&rt;");
  }
 else   if (c > 127) {
    buffer.append("&#" + ((int)c) + ";");
  }
 else {
    buffer.append(c);
  }
}
