{
synchronized (contributionPanels) {
    for (    ContributionPanel panel : contributionPanels) {
      if (panel.info == oldInfo) {
        panel.info=newInfo;
        panel.updateState();
      }
    }
  }
}
