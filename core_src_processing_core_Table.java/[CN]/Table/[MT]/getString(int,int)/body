{
  if (row >= getRowCount()) {
    return null;
  }
  if (!typed) {
    if (data[row] == null) {
      return null;
    }
    if (column >= data[row].length) {
      return null;
    }
    return data[row][column];
  }
 else {
  }
}
