{
  String line=null;
  int row=0;
  setRowCount(10);
  while ((line=reader.readLine()) != null) {
    if (row == getRowCount()) {
      setRowCount(row << 1);
    }
    setRow(row,splitLineCSV(line));
    row++;
  }
  if (row != getRowCount()) {
    setRowCount(row);
  }
}
