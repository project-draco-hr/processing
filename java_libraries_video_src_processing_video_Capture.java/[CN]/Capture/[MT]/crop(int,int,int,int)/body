{
  crop=true;
  cropX=Math.max(0,x);
  cropY=Math.max(0,y);
  cropW=Math.min(w,dataWidth);
  cropH=Math.min(dataHeight,y + h) - cropY;
  if ((cropW != width) || (cropH != height)) {
    init(w,h,RGB);
  }
}
