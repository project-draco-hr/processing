{
  new Thread(){
    public void run(){
      prepareRun();
      try {
        amode.handleRunDevice(sketch,AndroidEditor.this);
      }
 catch (      SketchException e) {
        statusError(e);
      }
catch (      IOException e) {
        statusError(e);
      }
    }
  }
.start();
}
