{
  pushMatrix();
  translate(x1,y1);
  int imageWidth=image.width;
  int imageHeight=image.height;
  scale((x2 - x1) / (float)imageWidth,(y2 - y1) / (float)imageHeight);
  g2.drawImage(image.getImage(),u1,v1,null);
  popMatrix();
}
