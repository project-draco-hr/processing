{
  this();
  try {
    Reader r=parent.createReader(filename);
    if (r == null) {
      System.err.println("The file " + filename + " could not be found.");
      return;
    }
    parseFromReader(r);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
