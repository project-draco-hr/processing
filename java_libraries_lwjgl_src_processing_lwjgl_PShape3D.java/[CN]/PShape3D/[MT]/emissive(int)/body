{
  if (family == GROUP) {
    for (int i=0; i < childCount; i++) {
      PShape3D child=(PShape3D)children[i];
      child.emissive(rgb);
    }
  }
 else {
    colorCalc(rgb);
    emissiveFromCalc();
  }
}
