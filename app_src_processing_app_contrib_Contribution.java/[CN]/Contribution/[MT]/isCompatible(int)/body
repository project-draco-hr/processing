{
  if (compatibleVersions != null) {
    if (compatibleVersions.floorEntry(versionNum) != null && versionNum <= compatibleVersions.floorEntry(versionNum).getValue()) {
      return true;
    }
  }
  return false;
}
