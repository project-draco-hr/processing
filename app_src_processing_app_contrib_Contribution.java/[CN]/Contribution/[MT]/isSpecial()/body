{
  if (authors != null && authors.contains("The Processing Foundation")) {
    return true;
  }
  if (categories != null && categories.hasValue(SPECIAL_CATEGORY)) {
    return true;
  }
  return false;
}
