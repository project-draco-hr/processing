{
  if (parent.textAlign == CENTER) {
    x-=parent.textSize * width(c) / 2f;
  }
 else   if (parent.textAlign == RIGHT) {
    x-=parent.textSize * width(c);
  }
  if (z != 0)   parent.translate(0,0,z);
  parent.textImpl(c,x,y,z);
  if (z != 0)   parent.translate(0,0,-z);
}
