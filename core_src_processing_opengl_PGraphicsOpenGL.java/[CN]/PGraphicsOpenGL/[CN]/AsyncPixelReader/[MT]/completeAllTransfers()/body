{
  if (size <= 0)   return;
  boolean needEndDraw=false;
  if (!drawing) {
    beginDraw();
    needEndDraw=true;
  }
  while (size > 0) {
    endTransfer();
  }
  ongoingPixelTransfers.remove(this);
  if (needEndDraw)   endDraw();
}
