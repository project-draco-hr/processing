{
  arrayCheck();
  textures[size]=new WeakReference<PImage>(img);
  firstIndex[size]=firsti;
  lastIndex[size]=lasti;
  firstCache[size]=firstb;
  lastCache[size]=lastb;
  hasTextures|=img != null;
  size++;
}
