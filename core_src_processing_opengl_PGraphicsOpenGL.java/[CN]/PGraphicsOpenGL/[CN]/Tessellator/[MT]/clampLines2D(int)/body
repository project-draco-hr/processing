{
  boolean res=clamp2D();
  if (res) {
    int first=in.firstVertex;
    for (int ln=0; ln < lineCount; ln++) {
      int i0=first + 2 * ln + 0;
      int i1=first + 2 * ln + 1;
      res&=segmentIsAxisAligned(i0,i1);
    }
  }
  return res;
}
