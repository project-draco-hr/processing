{
  int ctx=pgl.getCurrentContext();
  glName=createVertexBufferObject(ctx,pgl);
  pgl.bindBuffer(PGL.ARRAY_BUFFER,glName);
  pgl.bufferData(PGL.ARRAY_BUFFER,size * INIT_VERTEX_BUFFER_SIZE * elementSize,null,PGL.STATIC_DRAW);
}
