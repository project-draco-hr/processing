{
  for (int i=first; i <= last; i++) {
    int index=4 * i;
    v.x=PApplet.min(v.x,pointVertices.get(index++));
    v.y=PApplet.min(v.y,pointVertices.get(index++));
    v.z=PApplet.min(v.z,pointVertices.get(index));
  }
}
