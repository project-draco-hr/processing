{
  for (int i=first; i <= last; i++) {
    int index=4 * i;
    v.x=PApplet.max(v.x,pointVertices.get(index++));
    v.y=PApplet.max(v.y,pointVertices.get(index++));
    v.z=PApplet.max(v.z,pointVertices.get(index));
  }
}
