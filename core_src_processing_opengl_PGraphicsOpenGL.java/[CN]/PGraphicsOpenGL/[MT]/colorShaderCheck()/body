{
  if (shaderWarningsEnabled && (polyTexlightShader != null || polyLightShader != null || polyTexShader != null)) {
    PGraphics.showWarning("Your shader cannot be used to render colored " + "geometry, using default shader instead.");
  }
}
