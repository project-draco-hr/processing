{
  if (img.width == 0 || img.height == 0) {
    return null;
  }
  if (img.parent == null) {
    img.parent=parent;
  }
  Texture tex=new Texture(this,img.width,img.height,params);
  setCache(img,tex);
  return tex;
}
