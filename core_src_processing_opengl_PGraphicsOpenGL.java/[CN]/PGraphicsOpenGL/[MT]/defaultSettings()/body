{
  super.defaultSettings();
  manipulatingCamera=false;
  clearColorBuffer=false;
  clearEveryFrame=true;
  textureMode(IMAGE);
  ambient(255);
  specular(125);
  emissive(0);
  shininess(1);
  setAmbient=false;
}
