{
  if (ptexture != null) {
    pgl.disable(PGL.BLEND);
    pgl.drawTexture(ptexture.glTarget,ptexture.glName,ptexture.glWidth,ptexture.glHeight,width,height,x,y,x + w,y + h,x,height - (y + h),x + w,height - y);
    pgl.enable(PGL.BLEND);
  }
}
