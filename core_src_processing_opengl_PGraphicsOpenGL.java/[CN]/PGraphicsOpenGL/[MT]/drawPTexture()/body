{
  Texture ptex=ptexture.get();
  if (ptex != null) {
    pgl.disable(PGL.BLEND);
    pgl.drawTexture(ptex.glTarget,ptex.glName,ptex.glWidth,ptex.glHeight,0,0,width,height);
    pgl.enable(PGL.BLEND);
  }
}
