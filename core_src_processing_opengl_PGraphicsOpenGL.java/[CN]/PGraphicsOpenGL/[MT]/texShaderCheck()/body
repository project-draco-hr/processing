{
  if (shaderWarningsEnabled && (polyFullShader != null || polyLightShader != null || polyFlatShader != null)) {
    PGraphics.showWarning("Your shader cannot be used to render textured " + "geometry, using default shader instead.");
  }
}
