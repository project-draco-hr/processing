{
  Texture tex=texture.get();
  if (tex != null) {
    pgl.disable(PGL.BLEND);
    pgl.drawTexture(tex.glTarget,tex.glName,tex.glWidth,tex.glHeight,0,0,width,height);
    pgl.enable(PGL.BLEND);
  }
}
