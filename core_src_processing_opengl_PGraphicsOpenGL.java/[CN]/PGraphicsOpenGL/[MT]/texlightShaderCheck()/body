{
  if (shaderWarningsEnabled && (polyLightShader != null || polyTexShader != null || polyColorShader != null)) {
    PGraphics.showWarning("Your shader cannot be used to render textured " + "and lit geometry, using default shader instead.");
  }
}
