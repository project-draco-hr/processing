{
  if (shaderWarningsEnabled && (polyFullShader != null || polyTexShader != null || polyFlatShader != null)) {
    PGraphics.showWarning("Your shader cannot be used to render lit " + "geometry, using default shader instead.");
  }
}
