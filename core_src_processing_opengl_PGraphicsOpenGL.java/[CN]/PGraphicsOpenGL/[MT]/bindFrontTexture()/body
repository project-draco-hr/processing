{
  if (primaryGraphics) {
    pgl.bindFrontTexture();
  }
 else {
    Texture ptex=ptexture.get();
    if (ptex == null) {
      createPTexture();
      ptex=ptexture.get();
    }
    ptex.bind();
  }
}
