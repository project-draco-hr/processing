{
  if (monoFont == null) {
    try {
      monoFont=createFont("DroidSansMono.ttf",size);
    }
 catch (    Exception e) {
      monoFont=new Font("Monospaced",Font.PLAIN,size);
    }
  }
  return monoFont;
}
