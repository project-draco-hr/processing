{
  if (Base.isMacOS()) {
    if (retinaProp == null) {
      Float prop=(Float)awtToolkit.getDesktopProperty("apple.awt.contentScaleFactor");
      retinaProp=prop == 2;
    }
    return retinaProp;
  }
  return false;
}
