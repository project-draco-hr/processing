{
  if (boldFont == null) {
    try {
      boldFont=createFont("DroidSans-Bold.ttf",size);
    }
 catch (    Exception e) {
      boldFont=new Font("SansSerif",Font.BOLD,size);
    }
  }
  return boldFont;
}
