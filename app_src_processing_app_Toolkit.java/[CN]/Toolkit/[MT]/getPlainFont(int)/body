{
  if (plainFont == null) {
    try {
      plainFont=createFont("DroidSans.ttf",size);
    }
 catch (    Exception e) {
      plainFont=new Font("SansSerif",Font.PLAIN,size);
    }
  }
  return plainFont;
}
