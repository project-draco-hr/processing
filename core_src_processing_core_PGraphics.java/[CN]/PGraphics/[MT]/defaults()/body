{
  colorMode(RGB,TFF);
  fill(TFF);
  stroke(0);
  shape=0;
  matrixStackDepth=0;
  rectMode(CORNER);
  ellipseMode(CENTER);
  textFont=null;
  textSize=12;
  textLeading=14;
  textAlign=LEFT;
  textMode=MODEL;
  if (parent != null) {
    background(204);
  }
  defaultsInited=true;
}
