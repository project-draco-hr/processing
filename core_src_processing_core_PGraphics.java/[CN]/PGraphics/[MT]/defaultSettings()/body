{
  colorMode(RGB,255);
  fill(255);
  stroke(0);
  shape=0;
  matrixStackDepth=0;
  rectMode(CORNER);
  ellipseMode(CENTER);
  textFont=null;
  textSize=12;
  textLeading=14;
  textAlign=LEFT;
  textMode=MODEL;
  if (mainDrawingSurface) {
    background(backgroundColor);
  }
  settingsInited=true;
}
