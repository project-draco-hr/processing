{
  if (primaryGraphics) {
    parent.smooth(quality);
  }
 else {
    if (settingsInited) {
      if (this.smooth != quality) {
        smoothWarning("smooth");
      }
    }
 else {
      this.smooth=quality;
    }
  }
}
