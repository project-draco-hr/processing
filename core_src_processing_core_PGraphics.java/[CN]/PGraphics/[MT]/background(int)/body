{
  if (((rgb & 0xff000000) == 0) && (rgb <= colorModeX)) {
    background((float)rgb);
  }
 else {
    rgb|=0xff000000;
    colorCalcARGB(rgb,colorModeA);
    backgroundFromCalc();
    clear();
  }
}
