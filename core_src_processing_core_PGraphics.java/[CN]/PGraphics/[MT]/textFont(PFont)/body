{
  if (which != null) {
    textFont=which;
    if (hints[ENABLE_NATIVE_FONTS]) {
      which.findNative();
    }
    textSize(which.size);
  }
 else {
    throw new RuntimeException(ERROR_TEXTFONT_NULL_PFONT);
  }
}
