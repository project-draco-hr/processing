{
  if (which != null) {
    textFont=which;
    if (hintEnabled(ENABLE_NATIVE_FONTS)) {
      which.findFont();
    }
    textSize(which.size);
  }
 else {
    throw new RuntimeException(ERROR_TEXTFONT_NULL_PFONT);
  }
}
