{
  if (format == RGB) {
    background(rgb);
  }
 else {
    if (((rgb & 0xff000000) == 0) && (rgb <= colorModeX)) {
      background((float)rgb,alpha);
    }
 else {
      colorCalcARGB(rgb,alpha);
      backgroundFromCalc();
      clear();
    }
  }
}
