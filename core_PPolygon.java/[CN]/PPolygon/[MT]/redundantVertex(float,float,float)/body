{
  if (vertexCount < 2)   return false;
  if ((Math.abs(vertices[vertexCount - 2][MX] - x) < 0.0001f) && (Math.abs(vertices[vertexCount - 2][MY] - y) < 0.0001f) && (Math.abs(vertices[vertexCount - 2][MZ] - z) < 0.0001f)) {
    vertexCount--;
    return true;
  }
  return false;
}
