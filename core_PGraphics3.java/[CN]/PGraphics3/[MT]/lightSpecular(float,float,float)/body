{
  if (lightCount < 1) {
    throw new RuntimeException("Must create a light before " + "calling lightSpecular().");
  }
  colorCalc(x,y,z);
  lightSpecularR[lightCount - 1]=calcR;
  lightSpecularG[lightCount - 1]=calcG;
  lightSpecularB[lightCount - 1]=calcB;
}
