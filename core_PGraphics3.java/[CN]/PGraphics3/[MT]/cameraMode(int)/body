{
  resetProjection();
  modelview.identity();
  inverseModelview.identity();
  if (mode == PERSPECTIVE) {
    perspective(cameraFOV,cameraAspect,cameraNear,cameraFar);
    lookat(cameraX,cameraY,cameraZ,cameraX,cameraY,0,0,1,0);
  }
 else   if (mode == ORTHOGRAPHIC) {
    ortho(0,width,0,height,-10,10);
  }
  cameraMode=mode;
}
