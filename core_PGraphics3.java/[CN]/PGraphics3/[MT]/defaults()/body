{
  super.defaults();
  manipulatingCamera=false;
  forwardTransform=modelview;
  reverseTransform=inverseModelview;
  perspective();
  textureMode(IMAGE);
  emissive(0.0f);
  specular(0.5f);
  shininess(1.0f);
}
