{
  if (useActive) {
    if (useStrategy) {
      render();
    }
 else {
      Graphics screen=canvas.getGraphics();
      if (screen != null) {
        screen.drawImage(g.image,0,0,width,height,null);
      }
    }
  }
 else {
    canvas.repaint();
  }
}
