{
  final AndroidProcess proc=processes.byPid(pid);
  if (proc == null) {
    System.err.println("Process " + pid + " ended, but I hadn't known about it.");
  }
 else {
    processes.refresh();
    firePropertyChange(APP_ENDED,proc,null);
  }
}
