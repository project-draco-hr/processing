{
  for (  final PropertyChangeListener pcl : Arrays.asList(pcs.getPropertyChangeListeners())) {
    pcs.removePropertyChangeListener(pcl);
  }
  outputListeners.clear();
  if (logcat != null) {
    logcat.destroy();
  }
  env.deviceRemoved(this);
}
