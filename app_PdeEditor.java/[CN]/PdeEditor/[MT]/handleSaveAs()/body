{
  doStop();
  message("Saving...");
  try {
    if (sketch.saveAs()) {
      message("Done Saving.");
      sketchbook.rebuildMenu();
    }
 else {
      message("Save Cancelled.");
    }
  }
 catch (  Exception e) {
    error(e);
  }
  buttons.clear();
}
