{
  if (presenting) {
    presentationWindow.hide();
  }
 else {
    try {
      appletLocation=engine.window.getLocation();
    }
 catch (    NullPointerException e) {
    }
  }
  if (running) {
    doStop();
  }
  try {
    engine.close();
    engine=null;
  }
 catch (  Exception e) {
  }
}
