{
  message("Saving...");
  try {
    if (sketch.save()) {
      message("Done Saving.");
    }
 else {
      message(EMPTY);
    }
    sketchbook.rebuildMenus();
  }
 catch (  Exception e) {
    error(e);
    checkModifiedMode=0;
  }
  buttons.clear();
}
