{
  try {
    String folder=file.getAbsolutePath();
    if (platform == WINDOWS) {
      Runtime.getRuntime().exec("explorer \"" + folder + "\"");
    }
 else     if (platform == MACOSX) {
      openURL(folder);
    }
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
