{
  InputStream is=openStream(filename);
  if (is != null)   return loadBytes(is);
  System.err.println("The file \"" + filename + "\" "+ "is missing or inaccessible, make sure "+ "it's been added to your sketch and is readable.");
  return null;
}
