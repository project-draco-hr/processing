{
  if (g != null) {
    size(iwidth,iheight,g.getClass().getName());
  }
 else {
    if (PApplet.JDK_VERSION >= 1.3) {
      try {
        Class c=Class.forName(JAVA2D);
        size(iwidth,iheight,JAVA2D);
        return;
      }
 catch (      ClassNotFoundException e) {
      }
    }
    size(iwidth,iheight,P2D);
  }
}
