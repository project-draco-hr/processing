{
  String filename=sketch ? (folder + File.separator + where) : where;
  File file=new File(filename);
  String parent=file.getParent();
  if (parent != null) {
    File unit=new File(parent);
    if (!unit.exists())     unit.mkdirs();
  }
  return filename;
}
