{
  mouseX=qmouseX;
  mouseY=qmouseY;
  if (fpsTarget != 0)   framerate_delay();
synchronized (g) {
    g.beginFrame();
    draw();
    dequeueMouseEvents();
    dequeueKeyEvents();
    g.endFrame();
  }
  update();
  pmouseX=mouseX;
  pmouseY=mouseY;
  frame++;
}
