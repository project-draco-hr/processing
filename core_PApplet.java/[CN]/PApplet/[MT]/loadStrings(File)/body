{
  try {
    return loadStrings(new FileInputStream(file));
  }
 catch (  IOException e) {
    System.err.println("problem loading strings from " + file);
    e.printStackTrace();
  }
  return null;
}
