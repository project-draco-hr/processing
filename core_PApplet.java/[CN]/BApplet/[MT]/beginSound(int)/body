{
  if (sonicInit)   return;
  sonicInit=true;
  if (l <= 0) {
    l=DEFAULT_LENGTH;
  }
  this.length=l;
  frequency=BSonic.SAMPLING_RATE;
  this.samples=new int[length];
  this.sonic=new BSonic(samples);
}
