{
  try {
    String folder=file.getAbsolutePath();
    if (Base.isWindows()) {
      Runtime.getRuntime().exec("explorer \"" + folder + "\"");
    }
 else     if (Base.isMacOS()) {
      openURL(folder);
    }
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
