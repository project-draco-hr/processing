{
  colorMode(RGB,TFF);
  fill(TFF);
  stroke(0);
  strokeWeight(ONE);
  background(204);
  shape=0;
  matrixStackDepth=0;
  rectMode(CORNER);
  ellipseMode(CENTER);
  arcMode(CENTER);
  angleMode(RADIANS);
  textFont=null;
}
