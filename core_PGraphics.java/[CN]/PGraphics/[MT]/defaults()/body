{
  colorMode(RGB,TFF);
  fill(TFF);
  stroke(0);
  strokeWeight(ONE);
  strokeCap(SQUARE);
  strokeJoin(MITER);
  background(204);
  shape=0;
  matrixStackDepth=0;
  rectMode(CORNER);
  ellipseMode(CENTER);
  angleMode(RADIANS);
  textFont=null;
  textSize=12;
  textLeading=14;
  textAlign=LEFT;
  textMode=OBJECT;
}
