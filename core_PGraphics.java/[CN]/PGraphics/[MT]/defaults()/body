{
  colorMode(RGB,TFF);
  fill(TFF);
  stroke(0);
  strokeWeight(ONE);
  background(204);
  shape=0;
  matrixStackDepth=0;
  rectMode(CORNER);
  ellipseMode(CENTER);
  angleMode(RADIANS);
  textFont=null;
  textSize=12;
  textLeading=14;
  textMode=ALIGN_LEFT;
  textSpace=OBJECT_SPACE;
}
