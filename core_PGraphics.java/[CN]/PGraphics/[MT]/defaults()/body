{
  colorMode(RGB,TFF);
  fill(TFF);
  stroke(0);
  strokeWeight(ONE);
  background(204);
  shape=0;
  noDepth();
  noLights();
  matrixStackDepth=0;
  lightEnable(0);
  lightAmbient(0,0,0,0);
  light(1,cameraEyeX,cameraEyeY,cameraEyeDist,255,255,255);
  textureMode(IMAGE_SPACE);
  rectMode(CORNER);
  ellipseMode(CENTER);
  arcMode(CENTER);
  angleMode(RADIANS);
  textFont=null;
}
