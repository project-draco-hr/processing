{
  textureImage=image;
  if (hints[NEW_GRAPHICS]) {
    if (z_order == true) {
      addTexture(image);
    }
 else {
      triangle.setTexture(image);
    }
  }
 else {
    polygon.texture(image);
  }
}
