{
  if (zbuffer != null) {
    for (int i=0; i < pixelCount; i++) {
      pixels[i]=backgroundColor;
      zbuffer[i]=MAX_FLOAT;
      stencil[i]=0;
    }
  }
 else {
    for (int i=0; i < pixelCount; i++) {
      pixels[i]=backgroundColor;
    }
  }
}
