{
  if (hints[NEW_GRAPHICS]) {
    if (z_order == true) {
      addTexture(image);
    }
 else {
      triangle.setTexture(image);
    }
    textureImage=image;
  }
 else {
    texture=true;
    polygon.texture(image);
  }
}
