{
  if ((dimensions == 0) && !lighting && !_tint&& (image_mode != CENTER_RADIUS)) {
    flat_image(image,(int)x1,(int)y1);
  }
 else {
    image(image,x1,y1,image.width,image.height,0,0,image.width,image.height);
  }
}
