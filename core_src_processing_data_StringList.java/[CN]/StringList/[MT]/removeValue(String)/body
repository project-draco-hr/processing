{
  if (value == null) {
    for (int i=0; i < count; i++) {
      if (data[i] == null) {
        remove(i);
        return true;
      }
    }
  }
 else {
    int index=index(value);
    if (index != -1) {
      remove(index);
      return true;
    }
  }
  return false;
}
