{
  if (which != null) {
    textFont=which;
    textSize(which.size);
    PApplet.println("size should be " + which.size);
  }
 else {
    throw new RuntimeException(ERROR_TEXTFONT_NULL_PFONT);
  }
}
