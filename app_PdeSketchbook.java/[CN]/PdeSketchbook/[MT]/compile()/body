{
  buildFileList();
  shouldCompile=new boolean[fileCount];
  int cacheCount=0;
  try {
    DataInputStream dis=new DataInputStream(new FileInputStream("lib/sketchbook/cache"));
    lastUpdate=dis.readLong();
  }
 catch (  IOException e) {
  }
}
