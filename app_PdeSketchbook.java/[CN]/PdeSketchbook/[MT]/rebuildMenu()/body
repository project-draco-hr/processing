{
  menu.removeAll();
  try {
    addSketches(menu,sketchbookFolder);
    menu.addSeparator();
    addSketches(menu,examplesFolder);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
  return menu;
}
