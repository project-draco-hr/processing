{
  if (outputKind == UNIX) {
    output.write(10);
  }
 else   if (outputKind == WINDOWS) {
    output.write(13);
    output.write(10);
  }
 else   if (outputKind == MAC) {
    output.write(13);
  }
}
