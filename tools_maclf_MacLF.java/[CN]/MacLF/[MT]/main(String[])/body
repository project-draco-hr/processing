{
  if (args.length == 0) {
    System.err.println("maclf <filename>");
    System.exit(0);
  }
  int kind=MAC;
  for (int i=0; i < args.length; i++) {
    File file=new File(args[i]);
    if (file.isDirectory()) {
      dir(file,kind);
    }
 else {
      load(file,kind);
    }
  }
}
