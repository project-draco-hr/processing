{
  byte[] returning=new byte[length];
  while (true) {
    int byteCount=input.read(returning,start,length);
    if (byteCount <= 0)     break;
    start+=byteCount;
    length-=byteCount;
  }
  return returning;
}
