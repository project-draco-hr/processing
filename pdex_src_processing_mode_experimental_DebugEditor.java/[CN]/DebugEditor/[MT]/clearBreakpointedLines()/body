{
  for (  LineHighlight hl : breakpointedLines) {
    hl.clear();
    hl.dispose();
  }
  breakpointedLines.clear();
  ta.clearLineBgColors();
  ta.clearGutterText();
  if (currentLine != null) {
    currentLine.paint();
  }
}
