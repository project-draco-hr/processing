{
  int count=0;
  for (  List<Handle> list : handles) {
    for (    Handle n : list) {
      if (n.type == "int" || n.type == "hex" || n.type == "webcolor") {
        count++;
      }
    }
  }
  return count;
}
