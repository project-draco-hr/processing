{
  statusNotice("Saving...");
  try {
    if (sketch.save()) {
      saved=true;
      statusNotice("Done Saving.");
    }
 else {
      statusEmpty();
    }
  }
 catch (  Exception e) {
    statusError(e);
  }
}
