{
  statusNotice("Saving...");
  try {
    if (sketch.saveAs()) {
      statusNotice("Done Saving.");
    }
 else {
      statusNotice("Save Canceled.");
      return false;
    }
  }
 catch (  Exception e) {
    statusError(e);
  }
  return true;
}
