{
  statusNotice("Saving...");
  try {
    if (sketch.saveAs()) {
    }
 else {
      statusNotice("Save Canceled.");
      return false;
    }
  }
 catch (  Exception e) {
    statusError(e);
  }
  return true;
}
