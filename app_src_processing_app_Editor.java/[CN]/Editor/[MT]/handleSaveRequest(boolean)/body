{
  if (untitled) {
    return handleSaveAs();
  }
 else   if (immediately) {
    handleSave();
  }
 else {
    SwingUtilities.invokeLater(new Runnable(){
      public void run(){
        handleSave();
      }
    }
);
  }
  return true;
}
