{
  buttons.activate(EditorButtons.SAVE);
  message("Saving...");
  try {
    if (sketch.save()) {
      message("Done Saving.");
    }
 else {
      message(EMPTY);
    }
  }
 catch (  Exception e) {
    error(e);
    checkModifiedMode=0;
  }
  buttons.clear();
}
