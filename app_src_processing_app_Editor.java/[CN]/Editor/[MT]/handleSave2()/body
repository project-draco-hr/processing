{
  toolbar.activate(EditorToolbar.SAVE);
  statusNotice("Saving...");
  try {
    if (sketch.save()) {
      statusNotice("Done Saving.");
    }
 else {
      statusNotice(EMPTY);
    }
  }
 catch (  Exception e) {
    statusError(e);
  }
  toolbar.deactivate(EditorToolbar.SAVE);
}
