{
  toolbar.activate(EditorToolbar.SAVE);
  message("Saving...");
  try {
    if (sketch.save()) {
      message("Done Saving.");
    }
 else {
      message(EMPTY);
    }
  }
 catch (  Exception e) {
    error(e);
  }
  toolbar.deactivate(EditorToolbar.SAVE);
}
