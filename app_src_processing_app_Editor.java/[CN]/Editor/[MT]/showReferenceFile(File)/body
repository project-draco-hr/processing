{
  try {
    file=file.getCanonicalFile();
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
  if (file.toString().endsWith(".txt"))   Base.openFolder(file);
 else   Base.openURL(file.toURI().toString());
}
