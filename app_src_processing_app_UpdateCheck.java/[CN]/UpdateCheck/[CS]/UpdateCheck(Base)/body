{
  this.base=base;
  new Thread(new Runnable(){
    public void run(){
      try {
        Thread.sleep(20 * 1000);
        updateCheck();
      }
 catch (      IOException e) {
      }
catch (      InterruptedException e) {
      }
    }
  }
,"Update Checker").start();
}
