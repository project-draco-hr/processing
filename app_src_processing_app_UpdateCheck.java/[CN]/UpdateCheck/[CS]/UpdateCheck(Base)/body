{
  this.base=base;
  if (isAllowed()) {
    new Thread(new Runnable(){
      public void run(){
        try {
          Thread.sleep(5 * 1000);
          updateCheck();
        }
 catch (        Exception e) {
        }
      }
    }
,"Update Checker").start();
  }
}
