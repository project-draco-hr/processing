{
  this.base=base;
  new Thread(new Runnable(){
    public void run(){
      try {
        Thread.sleep(10 * 1000);
        updateCheck();
      }
 catch (      Exception e) {
      }
    }
  }
).start();
}
