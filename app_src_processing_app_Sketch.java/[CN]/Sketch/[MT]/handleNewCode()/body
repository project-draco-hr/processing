{
  ensureExistence();
  if (isReadOnly()) {
    Base.showMessage(Language.text("new.messages.is_read_only"),Language.text("new.messages.is_read_only.description"));
    return;
  }
  renamingCode=false;
  promptForTabName(Language.text("editor.tab.rename.description") + ":","");
}
