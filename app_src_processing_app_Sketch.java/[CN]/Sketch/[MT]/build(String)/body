{
  String primaryClassName=preprocess(buildPath);
  Compiler compiler=new Compiler();
  if (compiler.compile(this,buildPath,primaryClassName,System.getProperty("sun.boot.class.path"))) {
    return primaryClassName;
  }
  return null;
}
